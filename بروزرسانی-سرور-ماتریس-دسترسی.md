# به‌روزرسانی سرور — «ماتریس دسترسی‌ها» بدون ریسک بیلد

> نگرانی درست شما: «نمی‌خواهم دوباره بیلد کنم، دفعهٔ قبل ایراد داد.»
> این راهنما طوری نوشته شده که **اگر بیلد شکست بخورد، سایت شما همچنان بالا بماند**.

---

## قانون طلایی: بیلد، سرویس در حال کار را نمی‌خواباند

`docker compose build app` فقط ایمیج جدید می‌سازد و **به کانتینر در حال اجرا دست نمی‌زند**.
سایت تا لحظه‌ای که خودتان `up -d` بزنید، با نسخهٔ قبلی سرویس می‌دهد.
پس بیلدِ ناموفق = هیچ اتفاقی نیفتاده.

آن چیزی که خطرناک است `docker compose up -d --build` است: کانتینر را می‌خواباند
و اگر بیلد بمیرد، سایت پایین می‌ماند. **این دستور را نزنید.**

---

## راه پیشنهادی — یک دستور، با بازگشت خودکار

```bash
cd /مسیر/مخزن/afagh
bash scripts/update-permissions.sh
```

این اسکریپت دقیقاً همان ترتیب امن را اجرا می‌کند:

| گام | کار | اگر شکست بخورد |
|---|---|---|
| ۱ | از ایمیج فعلی تگ `afagh-app:rollback-<تاریخ>` می‌گیرد | — |
| ۲ | `docker compose build app` | **توقف** — سایت دست‌نخورده با نسخهٔ قبلی بالاست |
| ۳ | seed دیتابیس با SQL خالص (بدون بیلد migrator) | توقف، سایت سالم |
| ۴ | فقط `app` را جایگزین می‌کند (`up -d --no-deps app`) | — |
| ۵ | ۹۰ ثانیه سلامت را چک می‌کند | **بازگشت خودکار** به ایمیج قبلی |

گزینه‌ها:
```bash
bash scripts/update-permissions.sh --db-only   # فقط دیتابیس، هیچ بیلدی
bash scripts/update-permissions.sh --no-pull   # کد را از گیت نگیر
```

---

## اگر می‌خواهید دستی و گام‌به‌گام پیش بروید

### گام ۱ — دیتابیس (بدون هیچ بیلدی، ۲ ثانیه)

جدول‌های `permissions` و `role_permissions` روی سرور شما **خالی**‌اند.
این فایل SQL آماده است و نیازی به ساختن ایمیج migrator ندارد:

```bash
cd /مسیر/مخزن/afagh
git fetch origin arena/01a07109-afagh && git checkout -f arena/01a07109-afagh

docker compose exec -T postgres psql -v ON_ERROR_STOP=1 -U afagh -d afagh_db \
  < afagh-next/scripts/seed-permissions.sql
```

خروجی موفق:
```
 permissions | grants | roles
-------------+--------+-------
          32 |     69 |    11
```

خصوصیات این SQL:
- **idempotent** — هر چند بار بزنید بی‌خطر است (تست‌شده: اجرای دوباره صفر تغییر می‌دهد).
- **تنظیمات دستی مدیر را پاک نمی‌کند** — نقشی که از قبل مجوز دارد اصلاً دست نمی‌خورد.
- در یک `BEGIN/COMMIT` است؛ هر خطایی = بازگشت کامل، دیتابیس نیمه‌کاره نمی‌ماند.

### گام ۲ — بیلد، بدون خواباندن سایت

```bash
docker image tag afagh-app:latest afagh-app:rollback-$(date +%F)   # بیمه
docker compose build app                                            # سایت هنوز بالاست
```

**اگر اینجا خطا داد، همین‌جا بایستید.** سایت شما دست‌نخورده است.
خطای رایج `exit code: 137` = کمبود حافظه:
```bash
sudo bash scripts/ensure-build-memory.sh     # ساخت swap
echo 'NODE_MAX_OLD_SPACE=1536' >> .env       # کاهش هیپ
docker compose build app                     # دوباره
```

### گام ۳ — سوییچ (چند ثانیه)

```bash
docker compose up -d --no-deps app
docker compose ps app                        # باید Up (healthy)
curl -I http://127.0.0.1:8080/login          # باید 200
```

### گام ۴ — اگر چیزی خراب بود، بازگشت فوری

```bash
docker image tag afagh-app:rollback-$(date +%F) afagh-app:latest
docker compose up -d --no-deps --force-recreate app
```
دادهٔ مجوزها در دیتابیس می‌ماند و هیچ ضرری ندارد (نسخهٔ قبلی اپ اصلاً آن جدول‌ها را نمی‌خواند).

---

## چرا اصلاً بیلد لازم است؟

چون صفحهٔ `/admin/permissions` قبلاً Mock بود و باید کدش عوض می‌شد:

| بخش | با seed دیتابیس درست می‌شود؟ | با بیلد درست می‌شود؟ |
|---|---|---|
| پرشدن کاتالوگ ۳۲ مجوز | ✅ | — |
| دکمهٔ «تأیید و ذخیرهٔ تغییرات» | ❌ | ✅ |
| نمایش ۱۱ نقش واقعی با تعداد کاربر واقعی | ❌ | ✅ |
| ماندگاری بعد از logout/login | ❌ | ✅ |

پس **هر دو** لازم است؛ ولی همان‌طور که بالا دیدید، بیلد را می‌شود بدون هیچ ریسکی انجام داد.

---

## بدون Docker (systemd / pm2)

```bash
cd /مسیر/مخزن/afagh && git pull --ff-only origin arena/01a07109-afagh
cd afagh-next
npm ci --omit=optional
npm run db:permissions          # seed دیتابیس (Node، بدون psql)
npm run build                   # ← اگر اینجا خطا داد، سرویس فعلی هنوز بالاست
sudo systemctl restart afagh    # فقط بعد از بیلد موفق
```

---

## بررسی نهایی

۱. با حساب مدیر وارد شوید → `/admin/permissions`
۲. باید ببینید: **۱۱ نقش واقعی · ۳۲ مجوز · ۶۹ تخصیص** و تعداد کاربر واقعی زیر هر ستون
۳. یک تیک بزنید → نوار بالا زرد می‌شود → «✅ تأیید و ذخیرهٔ تغییر دسترسی‌ها»
۴. خروج و ورود مجدد → تیک سر جایش است

```sql
SELECT action, "createdAt" FROM audit_logs
 WHERE action LIKE 'ROLE_%' ORDER BY id DESC LIMIT 5;
```

---

## اگر مرورگر گفت «This page couldn't load»

این پیام از اپ نیست؛ یعنی مرورگر اصلاً به سرویس نرسیده:

```bash
docker compose ps                      # app باید Up (healthy) باشد
docker compose logs app | tail -30
curl -I http://127.0.0.1:8080/login    # 200 = اپ سالم است، مشکل از Caddy/شبکه
```
نکته: مسیر `/` عمداً **307** به `/login` می‌دهد — این خطا نیست.

---

## فایل‌های مرتبط

| مسیر | نقش |
|---|---|
| `scripts/update-permissions.sh` | به‌روزرسانی یک‌دستوری با بازگشت خودکار |
| `afagh-next/scripts/seed-permissions.sql` | SQL آمادهٔ psql — بدون بیلد |
| `afagh-next/scripts/seed-permissions.mjs` | همان کار با Node (`npm run db:permissions`) |
| `afagh-next/scripts/gen-permissions-sql.mjs` | تولید دوبارهٔ SQL بعد از تغییر کاتالوگ |
| `afagh-next/src/lib/permissions-catalog.json` | منبع واحد ۳۲ مجوز و پیش‌فرض‌ها |
