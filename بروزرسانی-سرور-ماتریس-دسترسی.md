# به‌روزرسانی سرور — فقط «ماتریس دسترسی‌ها» (`/admin/permissions`)

این تغییر شامل **دو بخش** است و هر دو باید انجام شود، وگرنه صفحه خالی می‌ماند:

| بخش | چرا لازم است |
|---|---|
| کد اپ (`actions.ts` + `page.tsx` + کلاینت) | صفحه باید از دیتابیس بخواند و بنویسد → نیاز به **بیلد مجدد** |
| دادهٔ دیتابیس (کاتالوگ ۳۲ مجوز + نگاشت پیش‌فرض) | جدول‌های `permissions` و `role_permissions` روی سرور شما **خالی**‌اند → نیاز به **seed** |

---

## راه ۱ (پیشنهادی) — استقرار عادی Docker

اگر سرور با `docker compose` بالاست، هیچ کار دستی لازم نیست؛ سرویس `migrator`
خودش `seed-base.mjs` را اجرا می‌کند و seed مجوزها داخل آن قرار گرفته است:

```bash
cd /مسیر/مخزن/afagh
git fetch origin arena/01a07109-afagh
git checkout -f arena/01a07109-afagh
git pull --ff-only origin arena/01a07109-afagh
docker compose up -d --build
```

بررسی نتیجه:

```bash
docker compose logs migrator | grep "مجوزها"
# انتظار:  ✓ مجوزها (32) · تخصیص پیش‌فرض جدید: 69 · نقش دست‌نخورده (سفارشی): 0
```

> ⚠️ `docker compose up -d --build` کل ایمیج app را دوباره می‌سازد (روی سرور
> ۲ گیگی حدود ۲ تا ۵ دقیقه). اگر بیلد با `exit code: 137` مُرد، یعنی OOM:
> `sudo bash scripts/ensure-build-memory.sh` و سپس `NODE_MAX_OLD_SPACE=1536` در `.env`.

---

## راه ۲ — فقط seed دیتابیس، بدون بیلد مجدد

اگر کد را قبلاً به‌روز کرده‌اید و **فقط داده** کم دارید (یا نمی‌خواهید
`seed-base` کلیدهای cron را دوباره بسازد)، اسکریپت اختصاصی و بی‌خطر:

```bash
# داخل Docker
docker compose run --rm migrator node scripts/seed-permissions.mjs

# یا بدون Docker، روی خود سرور
cd afagh-next
DATABASE_URL='postgres://afagh:رمز@127.0.0.1:5432/afagh_db' npm run db:permissions
```

خروجی موفق:

```
🛡️  هم‌ترازسازی کاتالوگ مجوزها…
  ✓ مجوزها (32) · تخصیص پیش‌فرض جدید: 69 · نقش دست‌نخورده (سفارشی): 0
🎉 انجام شد — صفحهٔ «ماتریس دسترسی‌ها» اکنون داده دارد.
```

### ویژگی‌های این اسکریپت

- **idempotent است** — هر چند بار بخواهید اجرا کنید بی‌خطر است.
- **سفارشی‌سازی مدیر را پاک نمی‌کند**: نقشی که از قبل تخصیص دارد دست‌نخورده
  می‌ماند (در خروجی زیر «نقش دست‌نخورده (سفارشی)» شمرده می‌شود).
- کلیدهای `FINANCE_CRON_SECRET` / `GRAD_CRON_SECRET` / `BI_CRON_SECRET` را
  **بازتولید نمی‌کند** (برخلاف `seed-base.mjs`)، پس scheduler شما نمی‌شکند.
- اگر عمداً خواستید همه‌چیز به پیش‌فرض کارخانه برگردد:
  `node scripts/seed-permissions.mjs --reset-defaults` ⚠️ (تنظیمات دستی پاک می‌شود)

---

## راه ۳ — بدون Docker، سرویس systemd/pm2

```bash
cd /مسیر/مخزن/afagh
git pull --ff-only origin arena/01a07109-afagh

cd afagh-next
npm ci --omit=optional
npm run db:permissions          # ← seed مجوزها (بی‌خطر و تکرارپذیر)
npm run build
sudo systemctl restart afagh    # یا: pm2 restart afagh
```

---

## بررسی نهایی (بعد از هر سه راه)

۱. با حساب مدیر وارد شوید و به `/admin/permissions` بروید.
۲. باید ببینید: **۱۱ نقش واقعی · ۳۲ مجوز · ۶۹ تخصیص** و تعداد کاربر واقعی زیر هر نقش.
۳. یک تیک بزنید → نوار بالای صفحه زرد می‌شود → «✅ تأیید و ذخیرهٔ تغییر دسترسی‌ها».
۴. خروج و ورود مجدد → تیک باید سر جایش باشد.

بررسی مستقیم روی دیتابیس:

```sql
SELECT count(*) FROM permissions;        --> 32
SELECT count(*) FROM role_permissions;   --> ≥ 69
SELECT action, "createdAt" FROM audit_logs
 WHERE action LIKE 'ROLE_%' ORDER BY id DESC LIMIT 5;
```

---

## اگر مرورگر گفت «This page couldn't load»

این پیام از خود اپ نیست؛ یعنی مرورگر اصلاً به سرویس نرسیده است. به ترتیب:

```bash
docker compose ps                 # app باید Up (healthy) باشد
docker compose logs app | tail -30
curl -I http://127.0.0.1:8080/login   # باید 200 برگرداند
```

نکته: مسیر `/` عمداً **307** به `/login` می‌دهد — این خطا نیست.
اگر `app` بالا نیامده، معمولاً یکی از این دو است: بیلد OOM شده (`exit code: 137`)
یا `migrator` با خطا تمام شده و به‌خاطر `depends_on: service_completed_successfully`
اپ اصلاً استارت نشده — لاگ `migrator` را ببینید.
