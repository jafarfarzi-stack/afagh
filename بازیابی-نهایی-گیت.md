# 🚨 وضعیت فعلی گیت و برنامهٔ بازیابی — ۱۴۰۵/۰۶/۱۳

## ⚠️ نکتهٔ بحرانی که باید همین الان اصلاح شود

در push قبلی، شاخهٔ کار ما (`Next 14`) روی **main** ریخته شد. اما `main` رسمی شما (کامیت `530c796`)
**رسماً به Next.js 16 + React 19 مهاجرت کرده بود** (PR#2: «مهاجرت رسمی به Next.js 16 + React 19»).
یعنی الان `main` در GitHub = Next 14 است و مهاجرت Next 16 از main **حذف شده**. این حتماً باید اصلاح شود.

## ✅ وضعیت محلی (تأییدشده، آمادهٔ push)

| شاخه | محتوا | تأیید |
|---|---|---|
| `arena/01a05c13-afagh` (`b23f7db`) | همهٔ اصلاحات (P0/P1 بازبینی) روی خط Next 14 | tsc ✅ · تست ۹۸/۹۸ ✅ · build ✅ |
| `arena/01a05c13-afagh-next16` (`d8d5ec6`) | همان اصلاحات + **ارتقای رسمی به next@16.3.4 / react@19.2.8** | tsc ✅ · تست ۹۸/۹۸ ✅ · build ✅ |
| `tmp/remote-main` (`530c796`) | main اصلی شما (Next 16) — سالم نگه داشته شده | — |

## 🛠 سه دستور (همینها، با یک توکن جدید یا از ماشین خودتان)

```bash
# ۰) اگر توکن جدید دارید (Contents: Read and write):
#    TOK=github_pat_…   (یا git credential استفاده کنید)

# ۱) main = همهٔ اصلاحات + Next 16  ✅ (fast-forward از 530c796 — بدون force)
git push https://x-access-token:$TOK@github.com/jafarfarzi-stack/afagh.git arena/01a05c13-afagh-next16:refs/heads/main

# ۲) شاخهٔ Next 14 خط توسعه
git push https://x-access-token:$TOK@github.com/jafarfarzi-stack/afagh.git arena/01a05c13-afagh:refs/heads/arena/01a05c13-afagh

# ۳) شاخهٔ Next 16
git push https://x-access-token:$TOK@github.com/jafarfarzi-stack/afagh.git arena/01a05c13-afagh-next16:refs/heads/arena/01a05c13-afagh-next16

# تأیید نهایی:
git ls-remote --heads https://github.com/jafarfarzi-stack/afagh.git
#  → main و هر دو شاخه باید به کامیت‌های بالا اشاره کنند
```

> اگر ترجیح میدهید main به‌جای Next 16، همان Next 14 بماند: به‌جای دستور ۱،
> `git push <url> arena/01a05c13-afagh:refs/heads/main` بزنید (دوباره fast-forward است)
> — اما توصیهٔ ما: **دستور ۱** (main = همهٔ اصلاحات + Next 16 = دقیقاً «نصب بعد همه اجرا شود»).

## 📦 اگر دسترسی بالای ماشین ندارید

کل شاخهها در این workspace است؛ برای تحویل فایل:
- گزارش کامل: `گزارش-بازبینی-نهایی.md`
- فایل bundle قدیمی هم موجود است اما **منسوخ** است (قبل از این مرحله) — از آن استفاده نکنید؛
  بهترین تحویل، push با توکن تازه است (دستورهای بالا).
