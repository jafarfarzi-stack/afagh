# 🖱️ اجرای خودکار روی ویندوز — `afagh.cmd`

برای روشن کردن سامانه کافی است روی فایل **`afagh.cmd`** در ریشهٔ پروژه **دابل‌کلیک** کنید.
اسکریپت همهٔ مراحل را خودش انجام می‌دهد و در پایان مرورگر را روی
**http://localhost:8080** باز می‌کند.

## اسکریپت چه می‌کند؟

| مرحله | کار |
|---|---|
| ۱ | اگر **Docker Desktop** خاموش باشد خودش اجرایش می‌کند و منتظر Running شدنش می‌ماند |
| ۲ | اگر پورت PostgreSQL جزو **پورت‌های رزروشدهٔ ویندوز** باشد، خودکار پورت آزاد (مثلاً ۱۵۴۳۲) انتخاب و `docker-compose.yml` و `DATABASE_URL` را هماهنگ می‌کند |
| ۳ | کانتینرهای **PostgreSQL + Redis + MinIO** را بالا می‌آورد و صبر می‌کند آماده شوند |
| ۴ | `npm install` را **فقط اگر** `package-lock.json` عوض شده باشد می‌زند |
| ۵ | `db:push` و `db:hardening` را **فقط اگر** schema تغییر کرده باشد می‌زند |
| ۶ | `npm run build` را **فقط اگر** لازم باشد می‌زند |
| ۷ | سرور را اجرا و مرورگر را (وقتی واقعاً بالا آمد) باز می‌کند |

اجرای دوباره‌اش بی‌خطر است؛ مراحل غیرلازم را رد می‌کند، پس دفعات بعد چند ثانیه‌ای بالا می‌آید.

## سوییچ‌ها

از PowerShell در ریشهٔ پروژه:

```powershell
powershell -ExecutionPolicy Bypass -File .\afagh.ps1 [سوییچ]
```

| سوییچ | کار |
|---|---|
| `-Update` | اول `git pull` و بعد به‌روزرسانی وابستگی‌ها، دیتابیس و بیلد |
| `-Rebuild` | بیلد اجباری دوباره |
| `-Dev` | اجرا در حالت توسعه (`npm run dev`) با hot-reload |
| `-Stop` | فقط خاموش کردن کانتینرهای داکر (داده حفظ می‌شود) |
| `-Fresh` | ⚠️ حذف کامل داده‌ها و نصب از صفر |
| `-NoBrowser` | مرورگر را خودکار باز نکن |

می‌توانید سوییچ را به `afagh.cmd` هم بدهید:
`afagh.cmd -Update`

## ساخت میان‌بر روی دسکتاپ

روی `afagh.cmd` راست‌کلیک → **Send to → Desktop (create shortcut)**.
اگر خواستید آیکن یا سوییچ خاصی داشته باشد: راست‌کلیک روی میان‌بر → Properties →
در انتهای فیلد Target مثلاً ` -Update` را اضافه کنید.

## توقف

- **سرور وب**: در پنجرهٔ باز، `Ctrl+C` (یا بستن پنجره)
- **کانتینرهای داکر**: `afagh.cmd -Stop` یا `.\stop-docker.ps1`

## حساب‌های دمو (رمز همه: `123456`)

| نقش | نام کاربری |
|---|---|
| مدیر | `0000000001` |
| استاد | `0011111111` |
| دانشجو | `31412001` |

## عیب‌یابی

| مشکل | راه‌حل |
|---|---|
| «پورت 8080 از قبل اشغال است» | سامانه احتمالاً در حال اجراست؛ وگرنه `netstat -ano \| findstr :8080` و بستن آن PID |
| خطای دانلود ایمیج داکر (`no such host`) | Docker Desktop → Settings → Docker Engine → افزودن `"registry-mirrors": ["https://docker.arvancloud.ir"]` → Apply & Restart |
| بیلد بعد از `-Update` خطا داد | `afagh-next\node_modules` و `afagh-next\.next` را پاک و دوباره اجرا کنید |
| می‌خواهم مراحل از صفر کنترل شود | فایل `afagh-next\.afagh-state.json` را پاک کنید |
