# گزارش اجرای بازبینی مهندسی نهایی (P0 / P1 / P2)

> **تاریخ:** ۱۴۰۵/۰۶/۱۳ · **شاخه:** `arena/01a05c13-afagh` (Next 14) + **جدید:** `arena/01a05c13-afagh-next16` (Next 16 تأییدشده)
> **منبع:** بازبینی کامل reviewer («قبلاً 5.5–6/10، الان 7/10») — سه محور تعیینکننده: P0 اثبات IDOR، P1 RLS کامل، P1 CI اجباری، P1 Migration، P2 تست/Observability/DR

---

## ✅ P0 — اثبات: هیچ IDOR/Privilege Escalation در API/Server Action

**(۱) ممیزی کامل Actions و Routes** (این مرحله، دوباره روی نسخهٔ نهایی):

| مسیر | وضعیت |
|---|---|
| همهٔ `admin/*` ، `student/*` ، `professor/*` ، `alumni/*` ، `group-manager/*` | ✅ گارد سرور: `requireRole` / `requireDepHead` (با `staff.departmentId`) / `requireMigrationAdmin` |
| `group-manager` (۳ فایل با شمارش ۰) | ✅ تلهٔ مثبت کاذب — همه با `requireDepHead` گارد دارند |
| `id/actions` ، `verify-certificate/actions` ، `verify/[code]` ، `login` | ✅ عمداً عمومیاند + **rate-limit** (M-1) |
| `/api/admin/*` و `/api/cron/*` | ✅ `assertSameOrigin` (CSRF) + احراز secret/نقش |

**(۲) آزمون یکپارچهٔ RLS — `scripts/rls-test.mjs` (جدید)**: دقیقاً سناریوهای بازبینی روی دیتابیس واقعی:
- A ← SELECT دادهٔ B در ۱۲ جدول → **۰ ردیف** (students, users, enrollments, cart, notifications, requests, ledger, clearances, docs…)
- A ← UPDATE ردیف B → ۰ ردیف؛ DELETE سبد B → ۰ ردیف
- A ← INSERT برای B → **42501** (WITH CHECK)؛ INSERT اعلان برای B → 42501
- بدون `set_config` → همهچیز ۰ ردیف
- deny-all: `system_settings` (کلیدهای cron) و `audit_logs` → ۰ ردیف
- مجوز واقعی: A روی ردیف خودش `status` را عوض میکند ✅

**این آزمون در CI اجرا میشود** (job جدید `rls-integration` با PostgreSQL 16 واقعی — پایپلاین کامل migrator).

## ✅ P1 — RLS روی تمام دادههای حساس (نه فقط ۴ جدول) — ماتریس کامل در `pg-hardening.sql`

| گروه | جدولها | سیاست |
|---|---|---|
| هویت | `users`، `students`، `staff`، `sessions` | SELECT فقط ردیف خود (`app.user_id`) |
| آموزشی | `enrollments`، `cart_items`، `notifications`، `student_requests`، `transcript_snapshots`، `grade_appeals`، `seat_allocations`، `student_class_attendance` | فقط ردیف خود (از طریق دانشجو) |
| مالی | `student_ledger`، `financial_clearances`، `payroll_statements`، `professor_term_contracts` | فقط ردیف خود (پیمایش از staff) |
| اسناد/امن | `student_documents`، `military_service_records`، `kyc_verifications`، `electronic_documents`، OTPها و حضور/غیاب کارکنان | فقط ردیف خود |
| **deny-all** | `system_settings`، `integrations_config`، `audit_logs`، `api_audit_logs`، `admissions_staging`، `sanjesh_mappings`، `evaluation_responses`، `verification_otps`، `step_api_actions`، `document_signatures` | **RLS بدون سیاست = منع کامل** (رازها/لاگها هرگز توسط نقش اپ) |

**مجموع: ۳۸ جدول** (۲۸ حساس + ۱۰ deny-all). `hardening.mjs` همهٔ ۳۸ را در استقرار **اعتبارسنجی میکند** (در صورت نقص → exit 1 → اپ استارت نمیشود).

**گرنت ستونی (ضد Mass Assignment — بازبینی بند ۵):**
```sql
GRANT INSERT ON "enrollments" TO afagh_app;
GRANT UPDATE ("status", "waitlistPosition") ON "enrollments" TO afagh_app;  -- نه gradeValue/approvedBy/…
GRANT INSERT, DELETE ON "cart_items" TO afagh_app;
GRANT INSERT ON "notifications" TO afagh_app;
-- دانش‌آموز نمی‌تواند gradeValue، وضعیت مالی یا فیلدهای کارتابل را تغییر دهد (42501 در آزمون ثابت شد)
```

## ✅ P1 — CI اجباری (BLOCK قبل از Deploy)

`.github/workflows/ci.yml` اکنون ۳ job دارد:
1. **afagh-next**: `tsc → npm test → build → npm audit --audit-level=high` (شکست = قرمز — دیگر هیچ PR بدون تایپ/تست/اسکن امنیتی نمیگذرد)
2. **rls-integration** (جدید): PostgreSQL 16 → `drizzle push → patches → seed → hardening → rls-test` (اثبات P0 در هر پوش)
3. **afagh-erp**: `node --check` همهٔ فایلها

> `Dockerfile` همچنان `NEXT_SKIP_TYPE_CHECK=1` دارد — بهتنهایی قابل قبول است **چون CI (که گیت است) block میکند**؛ طبق سیاست بازبینی «فقط Docker build اجازهٔ ignore دارد».

## ✅ P1 — Migration با سناریوی شکست

| سازوکار | جزئیات |
|---|---|
| **پشتیبان پیش از استقرار** | `scripts/backup-db.mjs` (جدید): `pg_dump -Fc` قبل از همه چیز → `/backups/afagh-<ts>.dump` (حجم نصبشده: `afagh_backups`) |
| **قفل مهاجرت** | `pg_advisory_lock('afagh_migrator')` در `apply-patches` — دو migrator همزمان هرگز پچ نمیزنند |
| **شکست = توقف** | هر مرحله (backup/push/patches/seed/hardening) با exit-code غیرصفر → `depends_on: service_completed_successfully` → اپ شروع نمیشود (نسخهٔ قبلی اپ روی دیتابیس قبلی سالم میماند؛ بازیابی: `pg_restore -c --if-exists` از آخرین dump) |
| **ترتیب** | `backup → push → patches → seed → hardening` (Dockerfile) |

## ✅ P1/P2 — Demo Safety Gate + ابطال نشست + پایداری

- **گیت دومرحلهای دمو:** در production حتی `AFAGH_DEMO_MODE=1` بدون `AFAGH_ALLOW_INSECURE_DEMO=true` فعال نمیشود + **لاگ CRITICAL** در استارت (بند ۴ بازبینی).
- **ابطلال نشستها:** تغییر رمز → همهٔ نشستهای دیگر کاربر حذف میشوند (نشست جاری میماند)؛ `invalidateUserSessions(userId)` برای رویدادهای حساس (بند ۱۰).
- **پایداری Redis:** شنوندهٔ `error` روی هر دو کلاینت (rateLimit + waitingRoom) — قطعی موقت Redis دیگر Node را کرش نمیکند.
- **ایندکسهای نشست** (پاکسازی M-4) در schema + patches.
- **آزمون RLS در CI** (بالا) + `npm run test:rls`.

## ✅ ارتقای رسمی به Next.js 16 + React 19 (هماهنگ با main شما)

**کشف مهم:** `main` شما (کامیت `530c796`) رسماً `next@^16.3.4` + `react@^19.2.8` دارد (PR #2)، ولی خط توسعهٔ `arena/01a05c13-afagh` روی `next@14.2.35` بود. در این مرحله:
- شاخهٔ جدید **`arena/01a05c13-afagh-next16`** = همهٔ اصلاحات + ارتقا به Next 16 (همان نسخههای PR #2)
- **تأیید شده محلی:** `tsc` ✅ · `npm test` ۹۸/۹۸ ✅ · `npm run build` **موفق** ✅ (پس از اصلاح `params`/`searchParams` به Promise در ۹ صفحه/route — الزام Next 15+)
- بنابراین هر دو خط (Next14 با حفظ وضع فعلی، Next16 هماهنگ با main) در گیت پوشیده میشوند.

## ⚠ اقدام باقیمانده (نیازمند توکن تازه — قبلی revoke شده)

توکن قبلی پس از push ناموفق شد؛ برای فاینال نیاز به یک توکن جدید دارید (Contents: Read and write):
1. **بازیابی main به حالت رسمی:** `git push <url> 530c796…:main` — **یا بهتر:** شاخهٔ next16 به main (fast-forward، بدون force):
   `git push <url> arena/01a05c13-afagh-next16:main`  ← main = همهٔ اصلاحات + Next 16
2. `git push <url> arena/01a05c13-afagh:refs/heads/arena/01a05c13-afagh` (خط Next 14)
3. `git push <url> arena/01a05c13-afagh-next16:refs/heads/arena/01a05c13-afagh-next16`
