// سمت client نقش را نمی‌داند؛ سرور (layout ها + صفحهٔ ورود کامل) مرجع نهایی است.
// این تابع فقط مقصد اولیهٔ پس از ورود است — اگر نقش کاربر با ناحیه بخواند، همان‌جا می‌ماند.
export function homeForClient(): string {
  return '/';
}
