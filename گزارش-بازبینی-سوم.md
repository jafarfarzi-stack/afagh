# گزارش رفع ریسک‌ها — بازبینی سوم (Production Readiness ≈ 8.2/10)

تاریخ: ۱۴۰۵/۰۶/۱۳ — نسخهٔ اعمال‌شده: `48e6748` (Next 14) / `742925d` (Next 16)

## خلاصهٔ ارزیابی بازبین

| رده | یافته | وضعیت |
|---|---|---|
| 🔴 High | `requireStudentScope()` در حالت خطا/نقص داده **fail-open** بود | ✅ رفع شد (fail-closed مطلق) |
| 🟡 Medium | `status` مالی از ورودی کلاینت (`input.status`) | ✅ حذف — وضعیت ۱۰۰٪ سمت سرور |
| 🟡 Medium | حذف رکورد مالی بدون transition/audit (SELECT→DELETE) | ✅ سیاست حذف + Audit |
| 🟡 Medium | نبود Audit زنجیره‌ای (تمپرپروف) | ✅ `appendAudit` با sha256/prevHash |
| 🟡 Medium | پول float در مسیر مالی | ✅ `safeRials` — فقط عدد صحیح ریال |
| 🟡 بهبود | Migration «جای بهبود» | ✅ تأیید سلامت اسکیما در مهاجر |
| 🟡 بهبود | تست همزمانی واقعی approve/clearCheque | ✅ `concurrency-test.mjs` در CI |

## 🔴 High — fail-closed شدن `requireStudentScope()`

**قبلاً:** نبود `staff`/`student`/`departmentId`/`facultyId` (یا Exception داخلی) → `{ ok: true }` و ادامهٔ عملیات بدون scope (دسترسی‌پذیری دادهٔ همهٔ دپارتمان‌ها).

**حالا** (`src/lib/security.ts`):

- ADMIN (superuser) تنها استثناست — بقیهٔ نقش‌ها مشمول‌اند.
- هر یک از این حالت‌ها → **DENY صریح** با پیام فارسی + `console.error`:
  - کاربر نشست ندارد / role ثبت نشده / ستون دانشجویی (departmentId، facultyId، یا studentId) null است.
  - هر Exception در حین خواندن پروفایل.
- فقط تطبیق کامل نقش به ستون → `{ ok: true, scope }`.

**تأیید:** `tsc --noEmit` = ۰، `npm test` = ۹۸/۹۸، `audit-actions` = ۱۴۳/۱۴۳، `build` موفق در هر دو خط Next 14 و Next 16.

## 🟡 Mediumهای مالی (همه در `src/app/admin/finance/actions.ts`)

### ۱) وضعیت ۱۰۰٪ سمت سرور
- `addDiscountAction`/`addSponsorshipAction`: دیگر `input.status` ندارند. وضعیت از نوع سند تعیین می‌شود:
  - تخفیف: `requiresApproval ? 'PENDING' : 'APPROVED'`
  - پوشش: همیشه `PENDING`
  - وام: همیشه `PENDING`
- تایپ ورودی کلاینت اصلاح شد (`FinanceStudentClient.tsx` دیگر status ارسال نمی‌کند).

### ۲) سیاست حذف (حذف‌ناپذیری رکوردهای تکمیل‌شده)
| رکورد | حذف‌ناپذیر وقتی | پیام |
|---|---|---|
| تخفیف | `APPROVED` | «تخفیف تأییدشده قابل حذف نیست؛ طبق سیاست، فسخ آن باید با تنظیم مجدد ثبت شود» |
| پوشش | `CONFIRMED` یا `PAID` | مشابه |
| چک | `CLEARED` | «چک وصول‌شده قابل حذف نیست؛ بدهی از دفتر تسویه شده» |
| وام | `SETTLED` | «وام تسویه‌شده قابل حذف نیست» |

همهٔ حذف‌های مجاز داخل `db.transaction` + `appendAudit` (action: `FINANCE_*_DELETED`) انجام می‌شوند.

### ۳) Audit زنجیره‌ای (`src/lib/audit.ts`)
- `appendAudit(tx, entry)`: `hash = sha256(prevHash + actor + action + entityType + entityId + details + ts + ip)`.
- `prevHash` از آخرین رکوردِ همان `entityType` خوانده می‌شود → زنجیرهٔ قابل اثبات دست‌نخورده‌بودن.
- **فقط داخل تراکنش** صدا زده می‌شود — هیچ رویداد مالی بدون ثبت همزمان در دفتر و زنجیره ممکن نیست.
- IP ضبط می‌شود (همبستگی با گارد Origin).

### ۴) پول — `safeRials()` (`src/lib/money.ts`)
- فقط عدد صحیحِ نامنفیِ ریال (`Number.isSafeInteger`, regex `^\d{1,15}$`)؛ `1e5`، `123.456`، `NaN`، `Infinity`، ارقام فارسی → **null** → «مبلغ نامعتبر است».
- همهٔ توابع موجود (`unitsToInt`, `percentOf`, `ratioOf`, `sumUnits`, …) حفظ شدند.
- در `chargeByFormulaAction` مبلغ محاسبه‌شدهٔ موتور فرمول هم از `safeRials` می‌گذرد.

## 🟡 جای بهبود

### Migration — تأیید سلامت اسکیما (`scripts/apply-patches.mjs`)
پس از اعمال پچ‌ها و پیش از ادامهٔ استقرار، ۲۱ جدول حیاتی (`users`, `students`, `enrollments`, `student_ledger`, `payment_cheques`, `audit_logs`, …) با `to_regclass` بررسی می‌شوند؛ نقص → **exit 1 و توقف استقرار** (خطا در migrate، نه در runtime اپ).

### آزمون همزمانی واقعی (`scripts/concurrency-test.mjs` → CI)
با دو اتصال PG موازی، دقیقاً همان SQL شرطیِ اپ اجرا می‌شود و اثبات می‌شود **فقط یک برنده** دارد:
1. تأیید درخواست (`status IN (SUBMITTED,IN_REVIEW,DRAFT)` → APPROVED) — یک `rowCount=1`.
2. ثبت‌نام مشروط درس (`status <> REGISTERED`) — `enrolledCount` دقیقاً ۱.
3. وصول چک (`PENDING` → `CLEARED`) — یک تراکنش دفتر.
4. تأیید تخفیف (`PENDING` → `APPROVED`) — یک برنده.

(آزمون نیازمند PostgreSQL واقعی `DATABASE_URL` است؛ در Job یکپارچهٔ CI پس از `rls-test.mjs` اجرا می‌شود.)

## کارنامهٔ نهایی

| بررسی | Next 14 (`48e6748`) | Next 16 (`742925d`, main) |
|---|---|---|
| `tsc --noEmit` | ✅ ۰ | ✅ ۰ |
| `npm test` | ✅ ۹۸/۹۸ | ✅ ۹۸/۹۸ |
| `audit-actions.mjs` | ✅ ۱۴۳/۱۴۳ | ✅ ۱۴۳/۱۴۳ |
| `npm run build` | ✅ | ✅ |

## گام بعدی (طبق برنامهٔ بازبین)
- **ممیزی کامل** تمام Server Actions + API Routes + ماژول نمره/امتحان (از جمله `FINALIZE` نمره که سمت کلاینت است) برای IDOR / privilege escalation / race — خروجی: جدول P0/P1/P2 با فایل/خط/راه‌حل.
