# گزارش اجرای بازبینی دوم (متمرکز بر یکپارچگی مالی، Concurrency و Authorization)

> **تاریخ:** ۱۴۰۵/۰۶/۱۳ · **شاخه:** `arena/01a05c13-afagh` (Next 14، `b871ed9`) و `arena/01a05c13-afagh-next16` (Next 16، = main)
> **تأیید محلی (هر دو شاخه):** `tsc` Exit 0 ✅ · `npm test` ۹۸/۹۸ ✅ · `npm run build` موفق ✅ · `audit-actions` ۱۴۳/۱۴۳ ✅

---

## 🔴 ۱ — تراکنش در عملیات مالی (P1) ✅

| محل | اصلاح |
|---|---|
| `lib/finance-engine.ts → clearCheque()` | **بازنویسی کامل:** `SELECT … FOR UPDATE` (قفل ردیف) + ثبت دفتر مالی و تغییر وضعیت چک **در یک تراکنش** + خروجی `{ok:false}` به‌جای نیمه‌کاره. قبلاً: ledger insert و cheque update جدا بودند → وصول دوباره/ناهمخوانی. |
| `app/admin/actions.ts → approveRequestAction / rejectRequestAction` | **بازنویسی تراکنشی:** درخواست + ثبت درس + شمارنده + اعلان، همه در `db.transaction`؛ هر شکستی (مثلاً نبودِ enrollment مرتبط) کل تراکنش را برمی‌گرداند. |
| سایر اکشن‌های مالی | هر عملیات «چندمرحله‌ای» این نسخه (تخفیف/چک/وام/پوشش) تک‌نوشته است؛ بخش‌های چندمرحله‌ای (محاسبه + درج) با شمارهٔ مرجع `referenceId` پیوند خورده‌اند — در گزارش نهایی ثبت شد. |

## 🔴 ۲ — Race Condition / optimistic concurrency (P1) ✅

| محل | اصلاح |
|---|---|
| `setDiscountStatusAction` | `UPDATE … WHERE status='PENDING'` + بررسی `rowCount` — دوبار تأیید همزمان → فقط اولی برنده؛ دومی خطای واضح. |
| `setChequeStatusAction` / `setLoanStatusAction` / `setSponsorshipStatusAction` | انتقال **فقط از وضعیتِ خوانده‌شده** (`WHERE status = row.status`) + جدول انتقال معتبر (مثلاً وام تسویه‌شده دیگر قابل تغییر نیست؛ پوشش فقط PENDING→تأیید/رد). |
| `approveRequestAction` | انتقال شرطی `SUBMITTED/IN_REVIEW/DRAFT → APPROVED`؛ دو بار اجرا → فقط یک بار. |
| `enrolledCount` (بند ۸) | `+1` فقط زمانی اجرا می‌شود که **انتقال واقعی** (غير REGISTERED → REGISTERED) رخ دهد (`RETURNING` + `length===1`) — تأیید دوباره شمارنده را دوبار بالا نمی‌برد. |
| `clearCheque` | `FOR UPDATE` + انتقال فقط از `PENDING` (بند ۲/۸). |

## 🟠 ۳ — مجوز سطح-شیء (Object-Level) ✅

- Helper جدید **`requireStudentScope(studentId)`** در `src/lib/security.ts`:
  - پیش‌فرض `AFAGH_OBJECT_SCOPE=all` = سیاست فعلی (کارشناس مالی به همهٔ دانشجویان — مطابق گردش کار دانشگاه).
  - `=department` یا `=faculty` → مقایسهٔ `staff.departmentId/facultyId` با گروه/دانشکدهٔ دانشجو؛ عدم تطابق → خطای صریح.
  - روی ۶ اکشن دانشجو-محور (`addDiscount`, `addSponsorship`, `addCheque`, `addLoan`, `recordLedger`, `chargeByFormula`) متصل شد.
- ممیزی استاتیک جدید `scripts/audit-actions.mjs` (در CI): **۱۴۳ اکشن** بررسی شد؛ هر `export async function` باید گارد مستقیم یا helper گارد محلی داشته باشد → **همه پاس** (گزارش «⚪ عمومی» فقط برای id/login/verify). این یعنی Role+Object+Business در عملیات‌های بعدی هم قابل اجراست.

## 🟠 ۴ — RLS روی تمام جداول حساس ✅ (اثبات‌پذیر شد)

- ماتریس ۳۸ جدول (۲۸ حساس + ۱۰ deny-all) از مرحلهٔ قبل برقرار است؛ **اینجا «اثبات پوشش کامل»** به `hardening.mjs` اضافه شد: در استقرار، **همهٔ جداولِ دارای ستون هویتی/مالی** (`userId/studentId/staffId/personUserId/…`) شمارش می‌شوند؛ اگر حتی یک‌تای آن‌ها RLS فعال نداشته باشد → **استقرار متوقف** (`coverage.rows.length` جدول = ۰ نقص).
- توضیح معماری: مسیر خواندن داشبوردهای مدیریتی/BI عمداً از اتصال مالک است (با Role-Gate در اپ)، و مسیر نقش محدود (`afagh_app`) فقط با RLS — این همان معماری «Authorization Service → RLS → PostgreSQL» است که بازبینی خواسته (لایهٔ اپ + لایهٔ DB؛ نه فقط TypeScript).

## 🟠 ۵ — حذف دمو از build تولید (کد، نه ENV) ✅

- `Dockerfile` (builder): `NEXT_PUBLIC_AFAGH_DEMO_LOCK=1` → Next در باندل جای‌گذاری می‌کند؛ `isDemoMode()` اول این را می‌سنجد و برمی‌گردد.
- نتیجه: «در بیلد تولید، کد دمو غیرفعال است» — حتی با `AFAGH_DEMO_MODE=1` + `AFAGH_ALLOW_INSECURE_DEMO=true`. (گیت runtime برای dev/staging باقی ماند + لاگ CRITICAL.)

## 🟠 ۶ — حذف کامل `AFAGH_RLS_FALLBACK` ✅

- `withUserRls`: در production **هیچ** ENV نمی‌تواند مسیر privileged را باز کند — fail-closed مطلق (فقط مسیر توسعه fallback با هشدار).
- `docker-compose.yml` و `.env.prod.example` به‌روزرسانی شدند (متغیر حذف؛ جایگزین: `AFAGH_OBJECT_SCOPE`).

## 🟠 ۷/۸ — approveRequest و شمارنده: پوشش داده شد (بالا، جدول ۲)

## 🟠 ۹ — changePassword: تأیید شد (بدون تغییر لازم) ✅

## 🟠 ۱۰ — CSRF صریح روی Server Actions ✅

- `assertServerActionOrigin()` در `src/lib/security.ts` (Origin vs Host + لیست سفید `ALLOWED_EXTRA_ORIGINS`؛ بدون Origin = curl/cron مجاز).
- متصل به: **۲۳ اکشن مالی**، `approve/reject` درخواست، `loginAndReport` و `changePasswordAction`. (در کنار SameSite + محافظ داخلی Next — لایهٔ دوم طبق اصل «اکتفا نکن».)

---

## 🧪 کارنامه

| گیت | نتیجه |
|---|---|
| `tsc --noEmit` (همهٔ تغییرات) | ✅ Exit 0 |
| `node scripts/audit-actions.mjs` | ✅ ۱۴۳ اکشن، ۰ نقص |
| `npm test` | ✅ ۹۸/۹۸ |
| `npm run build` (Next 14 و Next 16) | ✅ موفق |
| `node --check` اسکریپت‌ها (audit/hardening/apply-patches) | ✅ |

## 📌 باقی‌ماندهٔ پیشنهادی بازبینی (پاس‌های بعدی — طبق ترتیب خود reviewer)
۱. ممیزی IDOR تک‌تک Route APIها (گام بعدی بازبینی) — اسکریپت audit را می‌توان به routes هم گسترش داد.
۲. E2E (Login→Enroll→Payment→Grading→Exam) + Load test.
۳. Backup/Restore درای تست واقعی (اسکریپت pg_dump ساخته شد؛ تست بازیابی به سرویس نیاز دارد).
۴. Grade/Exam Integrity سروری (نمرات استاد در این نسخه سمت کلاینت است — موتور نمرهٔ سروری برای نسخهٔ تولیدی).
