# گزارش دور ۸ — ادامهٔ طرح: رفاکتور فرانت حجیم (نمرات + حقوق)

**تاریخ:** ۴ سپتامبر ۲۰۲۶ — **وضعیت: ✅ کامل، تست‌شده و push شده** — کامیت `c07f90e`

---

## ۱) چه چیزی طبق طرح انجام شد؟

طرح (سند Gemini) در این مرحله دو ماژول را هدف داشت: «نمرات استاد» و «حقوق و دستمزد» — شکستن مگاکامپوننت‌ها + `useReducer` + اتصال به Server Actions با `useActionState`/`useOptimistic`.

### A) ماژول نمرات استاد (`/professor/grades`) — ۱۹۴۴ خط → معماری تمیز

```
src/app/professor/grades/
├── page.tsx                 (سرور — فقط داده + فیکس searchParams)
├── types.ts                 (تمام تایپ‌ها — جدید)
├── grades-core.ts           (هستهٔ خالص: سقف بارم، نمرهٔ نهایی از ۲۰،
│                             ایزولهٔ درس مشترک، ارفاق، توزیع، هش ممیزی)
├── gradesReducer.ts         (تجمیع ~۱۶ useState در یک reducer قابل تست)
├── actions.ts               (Server Action امن — جدید)
├── ProfessorGradesClient.tsx (فقط Orchestrator: بنر/تب‌ها/toast)
└── components/
    ├── RosterTab.tsx        (useOptimistic + useActionState + مودال OTP)
    ├── RubricTab.tsx        (بارم‌بندی با ۴ الگوی آماده)
    ├── AppealsTab.tsx       (کارتابل اعتراض + مودال ایزوله + Server Action)
    ├── AnalyticsTab.tsx     (توزیع فراوانی)
    └── CertificateTab.tsx   (صورت‌جلسهٔ رسمی و چاپ بومی)
```

**نقاط کلیدی:**
- **RosterTab طبق گام ۳ طرح**: تایپ نمره → آپدیت آنی (Optimistic) + محاسبهٔ مجدد نمرهٔ نهایی در همان لحظه + ارسال پس‌زمینه (Background Sync) بدون قفل UI؛ این‌جا هیچ `useState` نمره‌ای وجود ندارد.
- **actions.ts (گام ۲ طرح)** — همهٔ مسیرها با `requireRole(['PROFESSOR'])` + اعتبارسنجی + نوشتن Drizzle + `revalidatePath`:
  - `saveGradeAction` (بازهٔ ۰..۲۰، سقف بارم، ایزولهٔ THEORY/LAB)
  - `submitTemporaryAction` / `requestFinalizeOtpAction` (ردیف `grade_submission_otps`) / `finalizeSignedAction` (هش ممیزی + `gradesFinalizedAt`)
  - `resolveAppealAction` (فقط OPEN، نمرهٔ ۰..۲۰، آپدیت `grade_appeals` + `enrollments`)
- **خودترمیمی دمو**: `src/lib/demo-grades-seed.ts` — `ensureGradePersistence` ردیف‌های ترم/درس/ارائه/دانشجو/ثبت‌نام را idempotent می‌سازد (فقط `isDemoMode()`؛ در production داده seed شده است). الگوی همان `ensureDemoStudentRecord`.
- **فیکس باگ Next 16**: `searchParams` در page.tsx باید `await` شود (خطای dev که پیدا شد، رفع شد).

### B) ماژول حقوق و دستمزد (`/admin/payroll` simulator) — ۲۳۹۶ خط → Orchestrator

```
src/app/admin/payroll/
├── PayrollEngineClient.tsx   (فقط شل: بنر/KPI/تب‌بار/مودال‌ها — ۶۸۹ خط)
├── payrollData.ts            (تایپ‌ها + ۷ مجموعهٔ INITIAL_*)
├── payrollReducer.ts         (وضعیت + منطق: گلوگاه‌ها، تسویه، نرخ‌ها، سیاست پرداخت)
└── components/               (۹ تب — هر کدام فقط رندر منطق خودش)
    ├── StatementsCartableTab.tsx      (گردش کار ۴ مرحله‌ای + فیلترها)
    ├── BiometricChainTab.tsx          (تردد بیومتریک و Chain Matching)
    ├── ElectronicDecreesTab.tsx       (ابلاغیه‌ها + یادآوری پیامکی)
    ├── InsuranceAdvancesTab.tsx       (بیمه/مساعده + تمین)
    ├── ExamAggregationTab.tsx         (قرنطینهٔ اوراق و بایگانی)
    ├── BaseRatesTab.tsx / MultipliersTab.tsx / ContractsTab.tsx / BankDisketteTab.tsx
```

- گلوگاه‌های قانونی تسویه (نمرات نهایی + امضای قرارداد/ابلاغیه) و `nextPayrollStatus` در reducer — **قابل Unit Test**.
- اتصال به دادهٔ واقعی از قبل در `LivePayrollClient` + `actions.ts` موجود است (مسیر دمو/شبیه‌ساز جداست؛ هم‌الگوی معماری).

## ۲) تست‌ها و تأیید

| مورد | نتیجه |
|---|---|
| `tsc --noEmit` روی کل پروژه | ✅ **TSC_EXIT=0** (صفر خطا) |
| `npm test` | ✅ **۱۷۱/۱۷۱** (۴۶ شهریه + ۹۸ مالی + **۲۷ جدید** grades-reducer) |
| ورود دموی پروف `0011111111/123456` از HTTP | ✅ |
| `GET /professor/grades` (+ `?offeringId=102`) | ✅ ۲۰۰، بدون خطا، تمام تب‌ها رندر شدند |
| `GET /admin/payroll?tab=simulator` (ادمین) | ✅ ۲۰۰، هر ۹ تب رندر شد |
| **E2E ذخیرهٔ نمره** (Server Action واقعی) | ✅ `ok:true` + ردیف‌های DB: `course_offerings(id=101)`، `enrollments(gradeValue=4.50, DRAFT)` با خودترمیمی کامل |
| **اعتبارسنجی منفی** (نمرهٔ ۸ با سقف بارم ۵) | ✅ رد شد: «نمرهٔ واردشده از سقف بارم (5) بیشتر است.» |

## ۳) ریموت

- main = `c07f90e` ✅ · `arena/01a05c13-afagh-next16` = `c07f90e` ✅
- ⚠️ `arena/01a05c13-afagh` (خط قدیمی Next14 + React 18) **عمداً به‌روز نشد**: این رفاکتور ذاتاً به React 19 (useOptimistic/useActionState) و Next 16 (searchParams Promise) وابسته است؛ cherry-pick آن شاخه را می‌شکست. باقی ماندن در `e8b73c8` تصمیم مهندسی است.

## ۴) گام‌های بعدی طرح (طبق سند)

1. **دادهٔ واقعی نمرات**: خواندن `initialOfferings` از دیتابیس (term/courses/offerings/enrollments) به‌جای mock — اکشن‌های فعلی از قبل برای همین طراحی شده‌اند.
2. اتصال `PayrollEngineClient` (شبیه‌ساز) به Server Actions موجود `payrollMidtermAction`/`payrollSettleAction` (یا انتقال آن به پایهٔ `LivePayrollClient`) — زمینه در `PayrollApi` آماده است.
3. الگوی همین جراحی برای بقیهٔ مگاکامپوننت‌ها: `CurriculumManagerClient` (۲۷۰۳)، `ExamPlanningClient` (۲۶۵۷)، `DepartmentPlanningClient` (۲۶۵۱)، `RegulationsClient` (۱۱۸۷)، `TemplateEngineClient` (۱۱۳۵).
4. (از گام‌های بعدی سند) لایهٔ زنده: WebSocket/SSE برای همگام‌سازی حضور و ایستگاه‌های کیوسک.
