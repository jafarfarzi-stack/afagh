# راهنمای جامع کاربری و راهبری سامانه دانشگاهی آفاق (Afagh ERP Enterprise User Manual)

> **نسخه مستند:** ۱.۴ (ویرایش سازمانی ۱۴۰۵)  
> **فایل رسمی پی‌دی‌اف:** [`docs/Afagh_ERP_Comprehensive_User_Manual.pdf`](./Afagh_ERP_Comprehensive_User_Manual.pdf)

---

## فهرست فصول راهنما
1. [فصل ۱: هسته هویتی، ساختار سازمانی و پذیرش سازمان سنجش](#فصل-۱-هسته-هویتی-ساختار-سازمانی-و-پذیرش-سازمان-سنجش)
2. [فصل ۲: میز خدمات الکترونیک و درخواست‌های دانشجویی (BPM)](#فصل-۲-میز-خدمات-الکترونیک-و-درخواستهای-دانشجویی-bpm)
3. [فصل ۳: اتصال به وب‌سرویس‌ها و همانندجویی پایان‌نامه ایرانداک (Service Tasks)](#فصل-۳-اتصال-به-وبسرویسها-و-همانندجویی-پایاننامه-ایرانداک-service-tasks)
4. [فصل ۴: مدیریت مهلت‌های زمانی (SLA)، نقشه حرارتی و پایش گلوگاه‌های اداری](#فصل-۴-مدیریت-مهلتهای-زمانی-sla-نقشه-حرارتی-و-پایش-گلوگاههای-اداری)
5. [فصل ۵: ماتریس پویای سطوح دسترسی و تفکیک وظایف (Dynamic RBAC)](#فصل-۵-ماتریس-پویای-سطوح-دسترسی-و-تفکیک-وظایف-dynamic-rbac)
6. [فصل ۶: سامانه آموزش مجازی و وبینار (BigBlueButton & Moodle SSO)](#فصل-۶-سامانه-آموزش-مجازی-و-وبینار-bigbluebutton--moodle-sso)
7. [فصل ۷: داشبوردهای زنده مدیریتی، گزارش‌های مقایسه‌ای و خودارزیابی پرسنل](#فصل-۷-داشبوردهای-زنده-مدیریتی-گزارشهای-مقایسهای-و-خودارزیابی-پرسنل)
8. [فصل ۸: حساب‌های کاربری نمونه و سوالات متداول (FAQ)](#فصل-۸-حسابهای-کاربری-نمونه-و-سوالات-متداول-faq)

---

## فصل ۱: هسته هویتی، ساختار سازمانی و پذیرش سازمان سنجش

### ۱.۱. زیرسیستم پردازش فایل سنجش و صف Staging
- ورود داده‌های داوطلبان به صورت متن خطی (TXT/CSV) شامل کدملی، نام، نام خانوادگی، کد رشته سنجش، سهمیه و رتبه.
- جدول واسط `admissions_staging` و تطبیق با جدول `sanjesh_mappings`:
  - **رکوردهای سبز (RESOLVED):** کدهای شناسایی‌شده در چارت دانشگاه و آماده ثبت‌نام.
  - **رکوردهای قرمز (PENDING_MAPPING):** رشته‌های ناشناخته با امکان نگاشت سریع با یک کلیک.
- **دکمه صدور دسته‌جمعی شماره دانشجویی و ثبت قطعی:** تبدیل مستقیم رکوردهای صف به کاربران رسمی (`users` و `students`).

### ۱.۲. موتور فرمول‌ساز پویای شماره دانشجویی
فرمول‌ها بر پایه توکن‌های استاندارد بدون دستکاری کد بک‌اند تعریف می‌شوند:
- `{Year:2}`: دو رقم آخر سال ورود (مثلاً `05` برای ۱۴۰۵).
- `{DegreeCode:1}`: کد مقطع (`1` کارشناسی، `2` ارشد، `3` دکتری).
- `{MajorCode:3}`: کد رشته در دانشگاه (مثلاً `412` مهندسی کامپیوتر).
- `{Seq:3}`: شمارنده ترتیبی خودکار (مثلاً `015`).
- **خروجی نمونه:** `051412015`.

---

## فصل ۲: میز خدمات الکترونیک و درخواست‌های دانشجویی (BPM)

### ۲.۱. گواهی اشتغال به تحصیل هوشمند
- اجرای بررسی شرایط خودکار (**Auto-Check**):
  1. بررسی ثبت‌نام فعال و حداقل واحد قطعی در نیمسال جاری.
  2. بررسی تراز مالی شهریه (عدم وجود بدهی).
- **صدور آنی:** تولید سند رسمی پی‌دی‌اف دارای سربرگ مصوب وزارت علوم، مهر، امضای دیجیتال و بارکد امنیتی SHA-256 قابل چاپ مستقیم.

### ۲.۲. تطبیق واحد و تسویه حساب موازی پنج‌گانه
- **تطبیق واحد:** پس از تایید مدیر گروه و آموزش کل، نمره درس تطبیق‌یافته مستقیماً در کارنامه آموزشی دانشجو ثبت و محاسبه می‌شود.
- **تسویه حساب موازی (Parallel Gateway):** استعلام همزمان ۵ بخش:
  1. امور مالی (بدهی = ۰)
  2. کتابخانه مرکزی (کتاب امانت = ۰)
  3. صندوق رفاه دانشجویان (تسویه وام)
  4. آزمایشگاه‌ها و کارگاه‌های تخصصی
  5. امور خوابگاه‌ها و اسکان

---

## فصل ۳: اتصال به وب‌سرویس‌ها و همانندجویی پایان‌نامه ایرانداک (Service Tasks)

### ۳.۱. استعلام برخط ایرانداک (Irandoc Plagiarism Check)
- در درخواست صحافی و مجوز دفاع، سیستم متد `POST` را به همراه کدملی و هش سند به API ایرانداک ارسال می‌کند.
- **منطق تصمیم‌گیری خودکار (Rule Engine):**
  - **مشابهت <= ۲۰٪ (مثلاً ۱۴.۲٪):** تأیید خودکار سیستمی، الصاق گواهی اصالت و انتقال به گام تعیین تاریخ دفاع.
  - **مشابهت > ۲۰٪ (مثلاً ۲۸.۵٪):** توقف پرونده و ابلاغ بازنویسی متن به دانشجو به علت تخطی از سقف مجاز.

---

## فصل ۴: مدیریت مهلت‌های زمانی (SLA)، نقشه حرارتی و پایش گلوگاه‌های اداری

### ۴.۱. سیاست‌های انقضای مهلت (Timeout Actions)
- **ارجاع خودکار (Escalation):** انتقال پرونده به مقام بالاتر در صورت تعلل کارشناس.
- **تأیید خودکار (Auto-Approve):** عبور سیستمی از گام به نفع دانشجو.
- **رد خودکار (Auto-Reject):** بستن پرونده در صورت عدم ارائه مدارک.
- **ارسال هشدار مکرر (Notify):** پیامک و نوتیفیکیشن پیگیری.

### ۴.۲. نقشه حرارتی مراحل (Process Step Heatmap)
- نمایش تفکیک رنگی زمان توقف واقعی پرونده‌ها در برابر سقف استاندارد (سبز=روان، زرد=هشدار تاخیر، قرمز=گلوگاه حاد).

---

## فصل ۵: ماتریس پویای سطوح دسترسی و تفکیک وظایف (Dynamic RBAC)

- **تفکیک وظایف (Segregation of Duties):**
  - کارشناس پذیرش (`REGISTRATION_STAFF`): دسترسی کامل به مدارک بدون دسترسی مالی و حقوق.
  - کارشناس مالی (`FINANCE_EXPERT`): تایید شهریه و مساعده بدون دسترسی به نمرات و چارت.

---

## فصل ۶: سامانه آموزش مجازی و وبینار (BigBlueButton & Moodle SSO)

- ورود یک‌کلیکه (1-Click SSO) بدون نیاز به نام کاربری و رمز عبور مجدد.
- تفکیک نقش استاد (Presenter/Moderator) و دانشجو (Attendee).
- آرشیو ویدیوها و پخش ضبط‌شده جلسات پیشین.

---

## فصل ۷: داشبوردهای زنده مدیریتی و خودارزیابی پرسنل

- **پایش زنده عملیاتی (Real-time Ops):** ردیابی پرونده‌های فوری و در آستانه نقض SLA.
- **گزارش مقایسه‌ای دانشکده‌ها (Comparative Benchmarking):** مقایسه سرعت اقدام (MTTR)، پایبندی به مهلت و رضایت دانشجو (CSAT).
- **کارنامه خودارزیابی پرسنل (`/professor/performance`):** نمایش حجم مختومه‌شده، رتبه در بین همکاران و نشان‌های بهره‌وری.

---

## فصل ۸: حساب‌های کاربری نمونه و سوالات متداول (FAQ)

| نقش کاربری | نام کاربری / کد ملی | کلمه عبور | اختیارات و دسترسی‌ها |
| :--- | :--- | :--- | :--- |
| **مدیر ارشد سیستم (Admin)** | `0000000001` | `123456` | دسترسی کامل، طراح فرآیندها، RBAC و پایش زنده |
| **عضو هیئت علمی / استاد** | `0011111111` | `123456` | کلاس مجازی، ثبت نمرات، خودارزیابی و قرارداد |
| **دانشجو (پورتال خدمات)** | `31412001` (یا `1010101010`) | `123456` | ثبت درخواست‌ها، گواهی اشتغال، کارنامه و LMS |
| **کارشناس آموزش کل** | `0055555555` | `123456` | بررسی پرونده‌ها، پذیرش سنجش و شورای آموزشی |
| **کارشناس امور مالی** | `0077777777` | `123456` | تسویه شهریه، تایید مساعده و حقوق اساتید |
