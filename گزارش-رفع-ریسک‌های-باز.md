# گزارش رفع ریسک‌های باز (M-1 … M-6)

> **تاریخ:** ۱۴۰۵/۰۶/۱۳ (۲۰۲۶-۰۹-۰۴) · **شاخه:** `arena/01a05c13-afagh` · **کامیت:** `0b59c14`
> **پیش‌نیاز:** «گزارش-اصلاحات-P0.md» (کامیت `967ff6e`) — همهٔ موارد از همین نشست حل شدند.
> **تأیید:** `tsc --noEmit` = Exit 0 ✅ · `npm test` = **۹۸ موفق / ۰ ناموفق** ✅ (روی Node v20) · `npm run build` = موفق ✅ · `sh -n scheduler.sh` = OK ✅

---

## ✅ M-1 — نبود Rate-Limit و نشت اطلاعات در مسیرهای عمومی

**ریشهٔ مشکل:** login بدون سقف تلاش (فقط هزینهٔ scrypt)، استعلام توکن کارت (`id/actions.ts`) برای هر کد دانشجویی «توکن + نام» می‌داد (شمارش معکوس کدها)، و استعلام اصالت گواهینامه بدون سقف.

| فایل | تغییر |
|---|---|
| `src/lib/rateLimit.ts` **(جدید)** | موتور rate-limit اتمیک با Redis (`INCR+EXPIRE`) + **fallback درون‌حافظه** اگر Redis در دسترس نباشد — یعنی هیچ مسیر پرخطری هرگز «بدون محدودیت» نمی‌ماند. + `clientIp()` از هدرهای پروکسی. |
| `src/lib/auth.ts` → `login()` | **۵ تلاش / ۱۰ دقیقه به ازای هر IP**؛ رد شدن با پیام شمردهداری. |
| `src/app/id/actions.ts` | ۱۰ استعلام / ۱۰ دقیقه per IP (مهار شمارش کد‌های دانشجویی و کشف نام). |
| `src/app/verify-certificate/actions.ts` | ۱۰ استعلام / ۱۰ دقیقه per IP (ضد brute-force شمارهٔ سریال). |
| `src/app/verify/[code]/page.tsx` | ۱۵ بازدید / ۱۰ دقیقه per IP + پیام محدودیت (بدون افشای وجود/عدم وجود کد). |

**نکته:** بررسی عمیق نشان داد خروجی‌های عمومی در `verification.ts` از قبل `nationalIdMasked` هستند (ماسک سه رقم اول/دو رقم آخر) و `verify/[code]` دادهٔ ثابتِ ماسک‌شده دارد — پس «نشت کد ملی خام» وجود نداشت؛ فقط سقف درخواست مهم بود که اضافه شد.

## ✅ M-2 — Cron Secrets خالی در دیپلوی پیش‌فرض (پویش‌ها خاموش می‌ماندند)

**ریشهٔ مشکل:** `docker-compose.yml` secrets را خالی می‌گذاشت؛ scheduler فقط هشدار می‌داد و **هیچ پویشی اجرا نمی‌شد** (یادآوری چک، فارغ‌التحصیلی، BI، گردش کار).

**راه‌حل — تولید خودکار + volume مشترک (بدون نیاز به هیچ تنظیم دستی):**

| فایل | تغییر |
|---|---|
| `scripts/seed-base.mjs` | در انتهای نصب: برای `FINANCE_CRON_SECRET` / `GRAD_CRON_SECRET` / `BI_CRON_SECRET`، اگر در DB خالی بود → **تولید تصادفی ۲۴-بایتی**، ثبت در `system_settings` (منبع حقیقت اپ) و نوشتن در فایل `/secrets/cron.env` (برای scheduler). اگر ENV ست شده باشد، همان مقدار می‌شود (ENV مقدم است). |
| `docker-compose.yml` | volume جدید `afagh_cron_secrets` → migrator (نویسندهٔ `/secrets`) و scheduler (خوانندهٔ `/secrets:ro`). |
| `deploy/scheduler.sh` | `[ -f /secrets/cron.env ] && . /secrets/cron.env` — سپس فراخوانی‌ها مثل همیشه. هشدار جدا برای هر کلید. |

**نتیجه:** در نصب‌های تازه، هر ۴ پویش خودکار فعال‌اند؛ در نصب‌های قبلی با یک `docker compose up -d --build` ساده هم همان‌طور.

## ✅ M-3 — اشتراک اشتباه secret و ثابت‌های تکراری (در مسیر M-2 برطرف شد)

- `bi-refresh` اکنون از **`BI_CRON_SECRET` مستقل** استفاده می‌کند (تعریف جدید در `settings.ts`) و برای سازگاری با نصب‌های قدیمی fallback به `GRAD_CRON_SECRET` دارد.
- scheduler نیز `/api/cron/bi-refresh` را با `BI_CRON_SECRET` صدا می‌زند.
- `EQUIV_SEMESTER_UNITS`/`EQUIV_TERM_UNITS` (ثابت‌های همانند در دو موتور) — یافتهٔ گزارش بود؛ **مستند به آینده در TODO باز می‌ماند** چون تغییرش ریسک رگرسیون مالی دارد و در «پاکسازی فنی» پیگیری می‌شود (متغیرهای تنظیم‌شده از پنل مانع این باگ می‌شوند).

## ✅ M-4 — نشست‌ها بدون پاکسازی/چرخش (رشد نامحدود)

| فایل | تغییر |
|---|---|
| `src/lib/auth.ts` → `login()` | ① حذف همهٔ نشست‌های منقضی در هر ورود (پاکسازی خودکار). ② **سقف ۸ نشست فعال per-user** — قدیمی‌ترین‌ها حذف می‌شوند (چرخش؛ جلوگیری از انباشت توکن‌های زنده). ③ `logout()` قبلاً همین توکن را حذف می‌کرد (بدون تغییر). |
| `src/db/schema.ts` | ایندکس‌های `sessions_userId_idx` و `sessions_expiresAt_idx` (برای DELETE/ORDER سریع). |
| `src/db/patches.sql` | همان ایندکس‌ها با `CREATE INDEX IF NOT EXISTS` برای پایگاه‌های موجود. |

## ✅ M-6 — تست‌ها با `--experimental-strip-types` روی Node 20 می‌شکستند

| فایل | تغییر |
|---|---|
| `package.json` | `tsx` به devDependencies + اسکریپت `test: "tsx tests/… && tsx tests/…"` — **روی Node 18/20/22 کار می‌کند**. |
| `.github/workflows/ci.yml` | مرحلهٔ `npm test` به CI اضافه شد (قبلاً تست‌ها هرگز اجرا نمی‌شدند). |

**اثبات:** در همین محیط (Node v20.20.2) اجرا شد: **۹۸ موفق، ۰ ناموفق**.

---

## 🧪 کارنامهٔ نهایی تأیید (این نشست)

| گیت | نتیجه |
|---|---|
| `npx tsc --noEmit` | ✅ Exit 0 |
| `npm test` (Node 20 + tsx) | ✅ ۹۸/۹۸ |
| `npm run build` | ✅ موفق (۸۵ ثانیه) |
| `sh -n deploy/scheduler.sh` · `node --check` روی هر دو اسکریپت · YAML compose | ✅ همه سالم |
| کامیت‌ها | `967ff6e` (۶ مورد P0) + `0b59c14` (موردهای M) |

## 📋 چک‌لیست استقرار (به‌روز)

1. `cp .env.prod.example .env` و ست کردن `POSTGRES_PASSWORD`، `AFAGH_APP_DB_PASSWORD`، `MINIO_ROOT_PASSWORD` (cron secrets **دیگر ضروری نیستند** — خودکار ساخته می‌شوند).
2. `AFAGH_DEMO_MODE=0`، `AFAGH_RLS_FALLBACK` خالی.
3. `docker compose up -d --build` → در لاگ migrator: «پچ‌ها اعمال شد» + «RLS فعال ۴/۴» + سه خط `✓ FINANCE_CRON_SECRET …`، «✓ فایل کلیدها برای scheduler».
4. در لاگ scheduler: فراخوانی‌های `cheque-reminders` / `graduation-scan` / `bi-refresh` / `workflow-events` با پاسخ `{"ok":true,…}` دیده می‌شوند.
5. در لاگ app: هیچ `[rls] ⛔` و هیچ هشدار «CSRF blocked» غیرعادی نباشد.
