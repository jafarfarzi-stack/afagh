# گزارش دور ۱۱ — شکستن مگاکامپوننت پنل دانشجویان (۱۷۴۵ خط → ۴ فایل) + تست جلالی/معدل

**تاریخ:** ۹ سپتامبر ۲۰۲۶ — **وضعیت: ✅ کامل، سبز، push شده**

ادامهٔ جدول «توصیه‌های باقی‌مانده» دور ۱۰: دو مگاکامپوننت بعدی `CurriculumManagerClient.tsx` (۱۸۳۱) و `StudentsManagerClient.tsx` (۱۷۴۵) بودند. در این دور پنل دانشجویان شکسته شد (تصمیم روش، بخش ۴).

---

## ۱) قبل و بعد

| | قبل | بعد |
|---|---|---|
| `StudentsManagerClient.tsx` | **۱۷۴۵ خط** (۹ `interface`، ~۲۰ تابع محاسباتی معدل/جلالی/حروف، نمای رسمی کارنامه، ۳ پنل، ۴ تب) | **۱۲۵۹ خط** (فقط state و سه پنل) |
| هستهٔ خالص قابل تست | صفر (همه داخل فایل `use client`) | **۲۲۶ خط** `transcript-utils.ts` + **۱۹۴ خط تست، ۸۶ assert** |
| نوع‌های مشترک | داخل فایل کلاینت؛ `page.tsx` مجبور بود `type` را از ماژول `use client` وارد کند | **۱۲۹ خط** `types.ts` (بدون React) |
| نمای چاپ/کارنامه | ۱۵۷ خط JSX وسط کامپوننت اصلی | **۱۶۷ خط** `components/OfficialTranscriptView.tsx` |
| صفحهٔ سرور | — | `page.tsx` فقط `type` را از `./types` می‌گیرد (نه از کلاینت) |

```
src/app/admin/students/
├── page.tsx                     294  (دست‌نخورده جز یک خط import type)
├── StudentsManagerClient.tsx   1259  ← state + سه پنل + تب‌ها
├── types.ts                     129  ← StudentItem / RegulationPick / Pagination / StaffItem / TermGroup / TranscriptSummary / CodeLabels
├── transcript-utils.ts          226  ← هستهٔ خالص: معدل، مشروطی، تفکیک نوع درس، جلالی، حروف فارسی
├── components/
│   └── OfficialTranscriptView.tsx 167 ← کارنامه رسمی سما (۳ نیمسال کنار هم + صفحهٔ دوم تفکیکی)
└── actions.ts                   185  (دست‌نخورده)
```

`tests/transcript-summary.test.ts` به انتهای `npm test` اضافه شد (اکنون ۱۸ سوئیت).

## ۲) چه چیزی جابه‌جا شد و چرا همان بقاست

* ۴۵۹ خط منطقِ نواحی منتقل‌شده با اسکریپت جدا و سپس دستی بررسی شد: **۹۸٫۰٪ عیناً کاراکتر‌به‌کاراکتر** جابه‌جا شده بود. ۹ خطِ باقی‌مانده دقیقاً و فقط: ۵ افزودن کلید `export`، ۴ خط مربوط به رفع باگ بخش ۳، و یک خط `export default`. هیچ منطقی بازنویسی نشد.
* `doPrintTranscript` عمداً در فایل کلاینت ماند (تنها کارش دست‌زدن به `document.body` و کلاس چاپ است — هستهٔ خالص نیست). کامنت توضیحی‌اش هم منتقل شد.
* رفتار رندر (دندانه‌بندی JSX در فایل جدید) تنها تغییر ظاهری است.

## ۳) یک باگ واقعی که همین‌جا بیرون افتاد (مثل دور ۱۰)

`faWords` — تبدیل معدل به حروف روی **کارنامهٔ رسمی** — ده‌تایی‌های رُند را خراب می‌ساخت، چون جدول اعداد فقط تا ۲۰ داشت و برای ۳۰..۹۰ به «بیست و (یکان)» برمی‌گشت:

| ورودی | قبل | بعد |
|---|---|---|
| ۱۲٫۵ | «دوازده و **بیست و صفر** صدم» ❌ | «دوازده و **پنجاه** صدم» ✅ |
| ۱۸٫۳ | «هجده و **بیست و صفر** صدم» ❌ | «هجده و **سی** صدم» ✅ |
| ۱۷٫۲۶ | «هفده و بیست و شش صدم» ✅ | همان ✅ |

رفع: جدول `FA_TENS` + تابع جدید `faIntWords(0..99)` که هم برای قسمت صحیح و هم اعشار به‌کار می‌رود؛ برای قسمت صحیح ≥ ۱۰۰ به رقم برمی‌گردد. هر دو حالت باگ‌شده در تست قفل شد.

## ۴) پوشش تست (۸۶ assert، همه سبز)

`numOrNull` (رشتهٔ خالی/بی‌ربط/`0`/`Infinity`) · `regThresholds` (پیش‌فرض ۱۰/۱۲، کانفیگ آیین‌نامه، محافظت در برابر `NaN`، صفرِ مشروع) · `passedCourseSet` (معاف/قبول‌بدون‌نمره همیشه، جبرانیِ قبول‌شده) · `summarizeTerm` (PENDING در واحد اخذه نیست، معاف فقط قبولی، معدل وزنی، آستانهٔ دلخواه) · `summarizeTotal` (سیاست `EXCLUDE_IF_PASSED` در برابر `KEEP_ALL` روی درس تکراری) · `groupTranscript` (ترتیب نیمسال، معدل هر نیمسال، مشروطیِ فایل مقدم بر محاسبه، ستون‌های تجمیعی، کارنامهٔ خالی، نیمسال بی‌کد `—`) · `courseTypeGroup` (نرمال‌سازی حروف عربی، پایان‌نامه/جبرانی/پایه، fallback) · `breakdownByType` (شش ستون به ترتیب جدول سما، مردودی واحد نمی‌آورد ولی در معدل نوع درس می‌ماند) · `faNum`/`faWords`/`codeLabel`.

**تاریخ جلالی:** الگوریتم ۳۳‌سالهٔ `g2j` روی **همهٔ روزهای ۲۰۲۰ تا ۲۰۲۶ (۲۵۵۷ روز)** در برابر تقویم جلالی خود ICU (`Intl … ca-persian`، `timeZone:'UTC'`) مقایسه می‌شود و **صفر اختلاف** است؛ به‌علاوه چهار پین دستی (نوروز ۱۴۰۳ و ۱۳۵۸، ۳۰ اسفند کبیسهٔ ۱۳۹۹، نبودِ ۳۰ اسفند در ۱۳۹۸). در بازهٔ ۱۹۷۰–۲۰۵۰ هم همین نتیجه گرفته شد (خارج از تست، برای اطمینان).

## ۵) تأییدها

| بررسی | نتیجه |
|---|---|
| `npx tsc --noEmit` | **۰ خطا** |
| `npm test` (۱۸ سوئیت) | **سبز** — از جمله ۸۶ assert جدید و ۸۶ assert برنامه‌ریزی دور قبل |
| `node scripts/audit-actions.mjs` | ۲۴۳ اکشن، **همه گارد Role/Object/Origin** (دست‌نخورده) |
| `npm run build` | **exit ۰** — ۹۲ مسیر، فقط ۵ هشدار همیشگیِ `api/archive/[docId]` |
| رندر واقعی روی PostgreSQL | **۳/۳ سناریو HTTP 200 و بدون هیچ خطای رندر** (جدول پایین) |
| `git status` پس از آزمون | تمیز؛ فایل‌های موقت پاک شدند |

دود‌آزمون رندر، مثل دور ۱۰، با PostgreSQL واقعی (`embedded-postgres` ۱۸٫۴ روی پورت ۵۴۳۳۰) + `next dev -p 8083` + نشست واقعی (کوکی که `proxy.ts` می‌پذیرد) اجرا شد:

| سناریو | ماکرهایی که در HTML پیدا شد |
|---|---|
| `/smoke-transcript` — رندر SSR نمای کارنامهٔ استخراج‌شده با دادهٔ فیکسچر | `کارنامه کل` · `این کارنامه بدون مهر…` · **`سیزده و بیست و پنج صدم`** (معدل ۱۳٫۲۵ به حروف — یعنی هستهٔ خالص و نما درست به هم وصل‌اند) · `فنی` |
| `/admin/students` (نشست مدیر) | `لیست دانشجویان` · `جستجو: شماره دانشجویی` |
| `/admin/students?q=1234&page=2&status=ALL&sort=id:asc` | همان‌ها + بدون خطای صفحه‌بندی/سورت |
| `/admin/students` بدون نشست | **۳۰۷ → `/login`** (نه ۵۰۰) |

`/smoke-transcript` و اسکریپت تولیدِ تقسیم، فایل‌های موقت بودند و پیش از کامیت حذف شدند (هرگز به ریپو راه نیافتند). هارنس در `/home/user/tmp-pg/smoke-students.mjs` بیرون از ریپو مانده و قابل استفادهٔ مجدد است.

## ۶) باقی‌ماندهٔ کار (توصیهٔ دور بعد)

1. `CurriculumManagerClient.tsx` (۱۸۳۱ خط، ~۳۸ `useState`، ۵ تب با `activeTab` در ۸۲۸/۸۸۵) — همان الگوی Provider دور ۱۰؛ هیچ تستی هم ندارد.
2. خودِ پنل دانشجویان هنوز ۱۲۵۹ خط است: سه پنل `mainView` (`students` ۶۷۵–۱۳۲۳، `professors` ۱۳۲۸–۱۶۴۰، `quick_menu` ۱۶۴۵–۱۷۴۱) و چهار بلوک `stuTab==='info_combined'`. **تقسیم مکانیکیِ تب‌ها اینجا ممکن نیست** (برخلاف برنامه‌ریزی): بلوک `info_combined` در چهار جای متفاوت و در هم تنیده است و تحلیل نام‌های آزاد به‌تنهایی برشِ ایمن نمی‌دهد؛ باید با قیچی دستی و تست رندر هر پنل انجام شود.
3. `getTranscript` (اکشن) هنوز هیچ تستی روی دادهٔ واقعی ندارد — یک fixture SQL کوچک در `rls-test.mjs` می‌تواند معدل کل را با فایل‌های مرجع وضع نمره بسنجد.
