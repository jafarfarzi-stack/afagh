# جمعبندی امروز + ادامهٔ طراحی سند «سامانه جامع آموزشی»

تاریخ: ۱۴۰۵/۰۶/۱۳ — مبنای کار: فایل ۶۴ صفحهای «طراحی سامانه جامع آموزشی - Google Gemini.pdf»

---

## بخش اول — سند چه میگوید و کجا متوقف شد؟

سند گفتوگویی است که در آن یک **ERP دانشگاهی** (آفاق) مرحلهبهمرحله طراحی و سپس با Code Review‌های تخصصی روی کد واقعی ارزیابی شده است. قوس کامل:

1. **موتور ETL & Reconciliation** — مهاجرت دادههای قدیمی (گلستان/سیدا/همآوا) با ۴ لایه: Staging، دیکشنری تطبیق (Mapping)، Bypass Validation نمرات گذشته (فریز قطعی)، مغایرتگیری مالی (خشکاجرا + پذیرش تراز افتتاحیه)
2. **زیرساخت مانیتورینگ/لاگ** — ۴ لایه (Pino ساختاریافته، Sentry، Loki/ELK، Prometheus/Grafana)
3. **ماژول فارغالتحصیلی (۲۱)** — تسویهٔ موازی + صدور مدرک (موقت دیجیتال/سجاد) + پورتال دانشآموختگان → ارتقا به **Zero-Touch Graduation** (ماشهٔ رویدادی، بدون دکمهٔ دانشجو)
4. **Code Review های متوالی** روی: regulations-engine، workflow-engine، payroll، bi، workflow-events (Outbox)، workflow-analytics، migration/importCodeMaps (Batch Insert)، Waiting Room Redis (ZSET)، verification.ts (رمزنگاری)، VerifyCertificateClient (فرانت)
5. **جمعبندی نهایی در ۴ گام اجرایی**: ① یکپارچگی/تراکنش DB ② ZSET Waiting Room ③ امنیت (چرخش نرم کلید، ماسک ثابت، پاکسازی نشست) ④ فرانت (React 19: useActionState + آپلود چندتکه)
6. **آخرین بخش سند**: انتقال تغییرات به چرخهٔ CI/CD (ci-cd.yml) — **جاییکه سند با «Limit reached» قطع میشود**؛ یعنی پیادهسازی گامهای ۲–۴ و CI/CD ناتمام ماند.

---

## بخش دوم — امروز در این گفتگو چه شد؟ (خلاصه)

### دور ۳ و ۴ — بازبینی مهندسی (همه push و CI-سبز شده)
- **High fail-open** در `assertServerActionOrigin` (catch → DENY + log) و `requireStudentScope` (fail-closed مطلق)
- **IDOR مالی id-based**: ۹ اکشن (تخفیف/پوشش/چک/وام) → `requireStudentScope(row.studentId)` پس از یافتن رکورد
- **وضعیت مالی سمت سرور**، **سیاست حذف**، **audit زنجیرهای sha256**، **safeRials**
- **رفع قرمزی واقعی CI** (که از ۲ دور قبل پنهان بود):
  - `drizzle-orm 0.36.4` → `0.45.2` (رفع SQL Injection — GHSA-gpj5-g38j-94v9)
  - `majors.majorCode` → unique (علت شکست seed-base)
  - **۲۱ جدول هویتی/مالی بدون RLS** → پوشش کامل (بخش ⑧ pg-hardening.sql)
  - `npm audit` مقاوم به ۵۰۳ رجیستری (`ci-audit.mjs`)
  - rls-test: ۵ باگ نهفته (هرگز در CI پاس نشده بود) → ۲۲/۲۲؛ concurrency-test: FK گمشده → ۴/۴
  - نتیجه: **CI واقعی ۳/۳ سبز** با تمام گامها (اثبات با /check-runs، چون get_commit_combined_status فقط stateهای قدیمی را نشان میداد)

### دور ۵ — دو «جای بهبود» بازبین در BPM
- `nextTrackingCode`: حلقهٔ ۵۰تایی → **دنبالهٔ DB** (`tracking_code_seq` + setval پسزمینه) — تأیید با Postgres واقعی WASM
- `processCache` محلی (TTL ۳۰s) → **حذف کامل** (سازگاری چند-نمونهای) + `invalidateProcessCache` پاکسازی شد
- فایل «بازبینی-کد-کلیدی-دور۵.md» با **متن خالص** کدها (حل مشکل باینری/ZIP برای بازبین)

---

## بخش سوم — ادامهٔ طراحی از محل توقف سند (همین دور — کامیت `dfec319`)

### گام ۲ — ZSET Waiting Room ✅ (مهمترین گلوگاه — همان «بله» سند)
| قبل (LIST) | بعد (ZSET) |
|---|---|
| `LPUSH/RPOP` + `LPOS` جایگاه | `ZADD score=timestamp` + `ZRANK` جایگاه O(log N) |
| جایگاه خطی O(N) با ۵۰۰۰ نفر | جایگاه در کسری از ms، بدون درگیری تکرشتهٔ Redis |
| `LPOP` | `ZPOPMIN` — خروج عادلانهٔ قدیمیترین |
| `LLEN` | `ZCARD` |

- کد واقعی `waitingRoom.ts` ارتقا یافت؛ **تست `waiting-room-zset.test.mts`** با همان ماژول واقعی (tsx)
- CI: **`redis:7-alpine`** به job یکپارچه اضافه شد + اجرای تست پس از concurrency-test
- ✅ **برنامهٔ تست ZSET در CI با Redis واقعی اجرا و سبز شد** (گام تأییدشده در لاگ ران)

### گام ۳ — امنیت ✅
- `maskNationalCode`: خروجی **طول ثابت ۱۰** (`3*****2`) — طول واقعی کد ملی لو نمیرود
- **Soft Key Rotation**: `verifyTokenSignature` حالا `PREVIOUS_TICKET_TOKEN_SECRET` را هم میپذیرد (timingSafeEqual) → چرخش کلید وسط دورهٔ امتحانات، کارتهای صادرشده را یکشبه باطل نمیکند
- (از قبل موجود: enforceSessionLimit، invalidateOtherSessions پس از تغییر رمز، invalidateUserSessions)

### گام ۴ — فرانت (React 19 / Next 16) ✅
- `VerifyCertificateClient`: از useState دستی (`isSearching/searchError/handleSearch`) به **`useActionState` + `useFormStatus`** (میانجی استاندارد سند)
- `src/lib/uploader.ts`: **آپلود چندتکهٔ ۲MB** (fileId + بازآرایی + progress) — آماده برای endpoint مهاجرت
- `postcss` → `8.5.28` (override سراسری) — حذف advisory high در وابستگی شاخهٔ Next14؛ main/Next16: ۰ high

### کارنامهٔ نهایی (هر دو خط)
| بررسی | Next14 `203567a` | Next16/main `dfec319` |
|---|---|---|
| tsc / test / audit-actions / build | ✅ ۰ / ۹۸/۹۸ / ۱۴۳/۱۴۳ / ✅ | ✅ ۰ / ۹۸/۹۸ / ۱۴۳/۱۴۳ / ✅ |
| CI گیتهاب (واقعی، با Redis) | — | ✅ **۳/۳ job سبز** — شامل `waiting-room-zset.test.mts` |

### ریموت (ls-remote)
- `main` = `dfec319` · `arena/01a05c13-afagh-next16` = `dfec319` · `arena/01a05c13-afagh` = `203567a`

---

## بخش چهارم — باقیمانده برای گام بعدی (صادقانه)
1. **CI/CD نهایی (بخش ۶ سند)**: job استقرار با `appleboy/ssh-action` + `backup-db.mjs` قبل از `docker compose up` + rolling update — نیازمند secrets (SERVER_HOST/USER/SSH_KEY) در تنظیمات ریپوی کاربر؛ روی main فعلاً ۳ job تست/ساخت سبز است.
2. **Endpoint سرور chunked upload** (`/api/admin/migration/chunk-upload`) — client-side helper آماده است.
3. **گسترش useActionState** به بقیهٔ فرمهای پرتکرار (پنل مهاجرت: TuitionTab، CodeMapTab، GradesTab) — الگو اثبات شد.
4. **پروفایلگیری فروشگاه/سرویس در prod** برای تصمیم Redis مشترک (فعلاً طبق سند، درستی بر بهینهسازیِ زودرس).
