# گزارش جمع‌بندی — اصلاح ۶ مورد Critical/P0

> **تاریخ:** ۱۴۰۵/۰۶/۱۲ (۲۰۲۶-۰۹-۰۳) · **شاخه:** `arena/01a05c13-afagh` · **کامیت:** `967ff6e`
> **مرجع:** گزارش بررسی قبلی — «گزارش-مهندسی-آمادگی-پروداکشن.md» (امتیاز ۶.۱/۱۰ «مشروط»)
> **حداقل معیار پذیرش:** تغییرات با `tsc --noEmit` تأیید شده‌اند ✅ (صفر خطا — ۲۰.۵ ثانیه، ۲۹۶ فایل)

---

## ✅ ① — RLS قطعی fail-closed (بدون fallback مخفی)

| فایل | تغییر |
|---|---|
| `afagh-next/src/db/index.ts` | `withUserRls` در production دیگر هرگز به اتصال مالک (BYPASSRLS) سقوط نمی‌کند؛ فقط `AFAGH_RLS_FALLBACK=1` صریح می‌تواند آن را فعال کند (با لاگ `[rls] ⛔`). گارد `requireEnvInProd` هم اضافه شد: در prod بدون `DATABASE_URL`/`DATABASE_URL_APP` پروسه متوقف می‌شود. |
| `afagh-next/src/db/pg-hardening.sql` | رمز هاردکد → placeholder `__AFAGH_APP_PASSWORD__` |
| `afagh-next/scripts/hardening.mjs` | بازنویسی: رمز از `AFAGH_APP_DB_PASSWORD` (در prod اجباری، بدون آن exit 1) + اعتبارسنجی پس از اجرا: RLS روی ۴ جدول + نقش بدون SUPERUSER/BYPASSRLS |

**مکانیزم:** در prod، `appDb` فقط به نقش محدود `afagh_app` وصل می‌شود؛ `set_config('app.user_id', …)` داخل تراکنش، عایق سطری را اعمال می‌کند. خطای زیرساخت (auth/perm/اتصال) → درخواست رد، نه bypass.

## ✅ ② — آپلود بایگانی: whitelist + سرو امن (رفع Stored XSS — یافتهٔ C-1)

| فایل | تغییر |
|---|---|
| `src/app/api/admin/archive/upload/route.ts` | whitelist سخت‌گیرانه: فقط `image/jpeg,png,webp,gif,bmp` و `application/pdf` (غیر آن → 415). `mimeType` ذخیره‌شده دیگر از کلاینت نیست؛ از همان مقدار تأییدشده است. |
| `src/app/api/archive/[docId]/route.ts` | سرو امن: نوع خارج از whitelist → `application/octet-stream` + **Content-Disposition: attachment**؛ همهٔ پاسخ‌ها: `X-Content-Type-Options: nosniff` + `Content-Security-Policy: sandbox`؛ گارد path traversal روی مسیرهای لوکال و کلید Object Storage؛ **XML-escape کامل** مقادیر interpolate در SVG پیش‌فرض (مثل `fileName` که از کلاینت می‌آید) |

**نکته:** حتی اگر رکورد قدیمی/آلوده `mimeType` خطرناک (مثل `text/html`) داشته باشد، هرگز `inline` رندر نمی‌شود — فقط دانلود با octet-stream.

## ✅ ③ — دفاع CSRF سراسری برای Route Handlerها (رفع C-2)

| فایل | تغییر |
|---|---|
| `src/lib/security.ts` (جدید) | `assertSameOrigin(req)`: Origin باید با Host هم‌مبدأ باشد (یا در `ALLOWED_EXTRA_ORIGINS`)؛ نبودِ Origin → بررسی Referer؛ نبودِ هر دو (cron/curl) → مجاز (CSRF مرورگری بدون Origin/Referer ممکن نیست). |
| ۱۱ route تغییردهنده | گارد در ابتدای هر handler: `admin/archive/upload`، `admin/archive/verify`، `admin/migration/{commit,import,dry-run,inspect}`، `admin/redis-warmup`، و POST هر ۴ cron endpoint (بی‌تأثیر روی crontab، چون curl هدر Origin نمی‌فرستد). |

**مکانیزم دفاعی:** کوکی `SameSite=None` (الزام HTTPS) چالش CSRF را باز می‌کرد؛ حالا هر POST/import/upload با مبدأ خارجی → 403.

## ✅ ④ — Secrets + شبکهٔ internal

| فایل | تغییر |
|---|---|
| `docker-compose.yml` | 🔴 **رفع باگ کشف‌شده:** `DATABASE_URL_APP` و `AFAGH_APP_DB_PASSWORD` هیچ‌جا در compose تعریف نشده بودند → اپ همیشه به `127.0.0.1` (خودش) می‌رفت و همیشه fallback مالک رخ می‌داد (یعنی RLS عملاً هرگز در Docker فعال نشده بود!). اکنون: `DATABASE_URL_APP=postgres://afagh_app:${AFAGH_APP_DB_PASSWORD}@postgres:5432/afagh_db` + `AFAGH_APP_DB_PASSWORD` + `AFAGH_DEMO_MODE` + `AFAGH_RLS_FALLBACK`. |
| `docker-compose.yml` | شبکهٔ جدید `dbnet` (internal): postgres/redis/minio/migrator فقط روی dbnet (بدون اینترنت)؛ اپ روی dbnet + پیش‌فرض (برای خروجی به درگاه/SMS). پورت‌های عمومی همان `127.0.0.1` (DNAT). |
| `.env.prod.example` | مستندسازی `AFAGH_APP_DB_PASSWORD`، `AFAGH_DEMO_MODE`، `AFAGH_RLS_FALLBACK`، `ALLOWED_EXTRA_ORIGINS` |

**اقدام استقرار لازم:** در `.env` مقدار قوی برای `AFAGH_APP_DB_PASSWORD` بگذارید؛ در غیر این صورت compose پیش‌فرض ناامن می‌گیرد (با کامنت هشدار).

## ✅ ⑤ — `DEMO_MODE` + رفع تضاد ریست رمز 123456

| فایل | تغییر |
|---|---|
| `src/lib/auth.ts` | `isDemoMode()`: فعال با `AFAGH_DEMO_MODE=1`؛ پیش‌فرض فقط خارج از production. `DEMO_PASSWORD` (قابل override با `AFAGH_DEMO_PASSWORD`). گیت روی: `ensureDemoUser`، پروویژن خودکار در `login`، و خودترمیم پروندهٔ دانشجو در `getStudentByUser`. 🔴 **تضاد ریست رمز رفع شد:** رمزی که کاربر عوض کرده دیگر در هر ورود دوباره ۱۲۳۴۵۶ نمی‌شود (فقط فعال‌سازی حساب غیرفعال در دمو). |
| `src/app/manual/page.tsx` | جدول حساب‌های دمو (فصل ۸) فقط در حالت دمو رندر می‌شود — در prod تبلیغ نشانی ورود وجود ندارد. |
| `src/app/professor/grades/ProfessorGradesClient.tsx` | fallback کد OTP (`12345`/`123456`) فقط با `NEXT_PUBLIC_AFAGH_DEMO_MODE=1` یا غیر-production پذیرفته می‌شود. |

**نکته:** `admissions-engine.ts` همچنان برای حساب‌های جدید «رمزد پیش‌فرض + `mustChangePassword=1`» می‌سازد — رفتار مقصود است (اجبار به تغییر در اولین ورود)، نه نقص. همین الگو در `import-legacy-data.mjs` رعایت شده است.

## ✅ ⑥ — انتقال auto-healing اسکیما به migrator

| فایل | تغییر |
|---|---|
| `src/db/patches.sql` (جدید) | ۱۱۲ خط پچ‌ها (جدول‌های کمکی + ایندکس یکتای `student_id_formulas`) — خارج از کد، یک منبع واحد. |
| `scripts/apply-patches.mjs` (جدید) | اجرای `patches.sql` روی `DATABASE_URL` با خروجی exit-code برای migrator. |
| `Dockerfile` | CMD migrator: `drizzle-kit push --force → apply-patches → seed-base → hardening` (ترتیب کامل پایپ‌لاین). |
| `src/db/index.ts` | `ensureDbSchemaPatches` در prod فقط warn؛ در dev با کاوش چند مسیر (`process.cwd()/src/db/patches.sql` و…). |

**نتیجه:** در production هیچ DDL در runtime اجرا نمی‌شود؛ migrator موظف است (و در `depends_on: service_completed_successfully` الزام شده).

---

## 🔎 یافته‌های جانبی که در همین مسیر رفع شد

1. **پیش‌نمایش SVG، تزریق `fileName`** (خانوادهٔ C-1): نام فایل از کلاینت می‌آید و بدون escape در SVG درج می‌شد → حالا XML-escape + `nosniff` + CSP.
2. **`DATABASE_URL_APP` تعریف‌نشده در compose** (یافتهٔ بررسی قبلی): ریشهٔ «fallback همیشگی» — حالا ست شده.
3. **`update.ps1` BOM** و mode فایل‌های shell — پاک‌سازی شد تا diff و اجرای deploy نام‌رسمی نماند.

## ⚙️ برای استقرار (چک‌لیست عملیاتی)

- [ ] `cp .env.prod.example .env` و ست کردن `POSTGRES_PASSWORD`، `AFAGH_APP_DB_PASSWORD`، `MINIO_ROOT_PASSWORD`، `FINANCE_CRON_SECRET`، `GRAD_CRON_SECRET`
- [ ] `AFAGH_DEMO_MODE=0` (پیش‌فرض compose همین است)
- [ ] `AFAGH_RLS_FALLBACK` خالی بماند (پیش‌فرض)
- [ ] اگر سرویس‌های پنل روی دامنهٔ دیگر iframe دارند: `ALLOWED_EXTRA_ORIGINS=https://…`
- [ ] `docker compose up -d --build` → تأیید لاگ migrator: «✅ پچ‌ها اعمال شد» و «RLS فعال ۴/۴ + نقش امن»
- [ ] بررسی لاگ app برای نبودِ `[rls] ⛔`

## 📌 ریسک‌های باز (خارج از این ۶ مورد — از گزارش قبلی)

| کد | موضوع | اولویت |
|---|---|---|
| M-1 | rate-limit روی login/استعلام‌ها + نشت `nationalId` در پاسخ verify-certificate | بالا |
| M-2 | cron secrets در compose پیش‌فرض خالی‌اند (پویش‌ها خاموش می‌مانند تا ست شوند) | بالا |
| M-4 | نشست‌ها بدون پاکسازی/چرخش (جدول `sessions` نامحدود رشد می‌کند) | متوسط |
| M-6 | تست‌های Node (`--experimental-strip-types`) روی Node 20 اجرا نمی‌شوند؛ در Docker (Node 22) درست‌اند؛ در CI باید اجرا شوند | متوسط |

## 🧪 نتایج تأیید

- `tsc --noEmit` → **Exit 0** (پس از همهٔ تغییرات)
- `node -v` = v20.20.2 → تست‌های unit در این محیط اجرا نمی‌شوند (M-6)
- `git log`: `967ff6e security(P0): ۶ اصلاح امنیتی …` روی شاخهٔ `arena/01a05c13-afagh`
