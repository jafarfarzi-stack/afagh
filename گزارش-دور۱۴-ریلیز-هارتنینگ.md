# دور ۱۴ — Release Hardening: رفع چهار P0 (و دو P1) مسیر پروداکشن

ورودی این دور، گزارش بررسی مهندسی کاربر بود (Docker، DB/RLS، Migration، Backup، CI/CD، Scheduler، Secrets، مسیر Production).
اول هر ادعا را روی همین شاخه (`arena/01a086f2-afagh`) راستی‌آزمایی کردم، بعد اصلاح کردم — چون دو ادعا روی این شاخه **کهنه** بود و یکی **بالاتر** از چیزی بود که نوشته شده بود.

## ۰) راستی‌آزمایی ادعاها

| ادعای گزارش | واقعیت روی این شاخه | نتیجه |
|---|---|---|
| P0-1 «`deploy-debian.sh` برای `AFAGH_APP_DB_PASSWORD` رمز تصادفی تولید نمی‌کند» | **نادرست/کهنه**: اسکریپت نصب سه رمز ۳۲تایی می‌سازد، `.env` را `chmod 600` می‌کند و برای نصب‌های قدیمی backfill دارد | تولید رمز بود؛ **اجباری نبودن** باقی بود: compose هنوز `:-afagh-app-pass` داشت، `make check-env` فقط سه مقدار را اسم‌می‌برد و `hardening.mjs` هر رمز غیرخالی را روی نقش می‌نشاند |
| P0-2 «/backups در migrator volume نیست» | **دقیقاً درست** ✓ | اصلاح شد (+ راستی‌آزمایی و retention که اصلاً وجود نداشت) |
| P0-3 «پروداکشن با `drizzle-kit push --force` مهاجرت می‌کند» | **درست** ✓ در حالی که CI از قبل `migrate-db.mjs` را می‌زد؛ یعنی CI ≠ Production دقیقاً همان‌جا | اصلاح شد |
| P0-4 «رمز اولیهٔ ثابت نمایش داده می‌شود» | **درست، ولی بدتر از گزارش**: خلاصهٔ نصب `رمز: 123456` و کدهای ملی دمو را چاپ می‌کرد، درحالی‌که در ایمیج تولید دمو در **زمان build** قفل است (`NEXT_PUBLIC_AFAGH_DEMO_LOCK=1`) — یعنی نصب پروداکشن عملاً **هیچ** حساب ورودی نداشت | رفع با «bootstrap حساب مدیر» + قفلِ قابل‌کنترل با build-arg |
| P1 «Scheduler وضعیت HTTP را تشخیص نمی‌دهد» | درست ✓ (`wget … \| head -c 400` کد خروج را دور می‌ریخت) | اصلاح شد |
| P1 «`ci-audit.mjs` در قطعی رجیستری exit 0 می‌دهد» | درست ✓ (عمدی و مستند بود) | دو سطحی شد: PR tolerant، دروازهٔ انتشار strict |
| P1 «اتصال مالک داخل کانتینر اپ نباشد» | درست ولی **بزرگ**: ۱۲۵ فایل `import { db } from '@/db'` دارند | آگاهانه باز گذاشتم — در سند «گیت‌ها» توضیح داده شد |

## ۱) P0-1 — سیاست رمز، یک منبع حقیقت، سه لایهٔ اجرا

- **جدید**: `afagh-next/scripts/lib/secret-policy.mjs` — طول حداقل (دیتابیس ۲۴ / آبجکت ۱۶)، ردِ
  پیش‌فرض‌های ضعیفِ شناخته‌شده، ردِ پیش‌شوندهٔ `CHANGE_ME*`، ردِ تکرار چهارتایی، `generateSecret()`،
  و CLI: `--check <file>` و `--gen [len]`.
- `make check-env` از همان CLI تغذیه می‌شود (به‌جای four copyهای دستیِ قبلی).
- `hardening.mjs` **پیش از** `ALTER ROLE afagh_app` سیاست را می‌سنجد؛ نقض = توقف کل استقرار.
- **لایهٔ نهایی داخل کد اپ**: `src/lib/secret-guard.ts` که از `src/db/index.ts` قبل از ساختن هر Pool صدا
  زده می‌شود — در `NODE_ENV=production` با رمز ضعیف (حتی اگر فقط داخل `DATABASE_URL_APP` باشد) **بوت بسته
  می‌شود**؛ `ALLOW_WEAK_SECRETS=1` آن را به هشدار تبدیل می‌کند (توسعه/دمو) — ولی مقدار خالی یا کوتاه‌تر از حد، حتی با این flag هم پذیرفته نمی‌شود.
- `docker-compose.yml`: همهٔ `:-`های رمز دیتابیس حذف و به `${VAR:?پیام فارسی}` تبدیل شد →
  `docker compose up` بدون `.env` **بالا نمی‌آید** (پیام، دستور را هم می‌گوید).
- `deploy-debian.sh`: حداقل طول ۱۶ → **۲۴** (MinIO ۸ → ۱۶) + تست تکرار کاراکتر + هم‌نام‌سازی فهرست ضعیف‌ها.

**اثبات زنده** (روی PostgreSQL واقعی که در این دور ساختم):

```
hardening با AFAGH_APP_DB_PASSWORD=afagh-app-pass (production) → exit 1
  ❌ AFAGH_APP_DB_PASSWORD یکی از پیش‌فرض‌های ضعیف شناخته‌شده است…
  ✓ نقش afagh_app ساخته نشد (pg_roles: n=0)
hardening با رمز ۳۲تایی → ✅ RLS فعال است (41/41 جدول) · گرنت ستونی · پوشش ۵۴ جدول
گارد بوت اپ (tsx، NODE_ENV=production):
  رمز ضعیف            → ✅ بسته شد: [db] 🔒 … پیش‌فرض‌های ضعیف
  فقط داخل URL ضعیف    → ✅ بسته شد (از DATABASE_URL_APP هم می‌خواند)
  سه سکرت قوی          → ✅ بوت مجاز
  ALLOW_WEAK_SECRETS=1 → ⚠ هشدار + بوت مجاز
```

## ۲) P0-2 — پشتیبان: ماندگار، راستی‌آزمایی‌شده، چرخشی

- compose: volume جدید `afagh_backups:/backups` روی `migrator` (پیش‌فرض `AFAGH_BACKUP_DIR=/backups`).
- `backup-db.mjs`: نوشتن در اولین مسیرِ **نوشتنی** از `AFAGH_BACKUP_DIR → /backups → ./backups`
  (`pickOutDir` با پروب واقعی)؛ بعد از dump: `pg_restore --list` + حداقل حجم + `sha256` کنارآمد +
  فایل `LATEST` + retention (`AFAGH_BACKUP_KEEP`، پیش‌فرض ۱۴). **شکستِ راستی‌آزمایی = توقف استقرار**
  و حذف فایل بی‌اتکا.
- **جدید**: `scripts/verify-backup.mjs` — سه سطح: امضا، ساختار آرشیو، و `--restore-db`/`--restore-url`
  که پشتیبان را در یک دیتابیس موقت **واقعاً بازگردانی** می‌کند، شمار ردیف جدول‌های حساس را با مبدأ
  مقایسه می‌کند و بررسی می‌کند RLS بعد از بازیابی فعال است؛ دیتابیس موقت را هم پاک می‌کند.
- میان‌بُرهای تازه: `make backup` · `make verify-backup` · `make drill` ·
  `make copy-backups-to-host DEST=…` · `make restore-dump FILE=…`.
- **CI**: گام «DR drill» به job ریسندگی اضافه شد — یعنی بازگردانی در هر push با pg_dump/pg_restore
  واقعی آزموده می‌شود، نه فقط وجود کد.

**اثبات زنده**: retention روی ۶ فایل (۳ آخر ماند ✓)، sha256 مطابقت ✓، و آزمون منفیِ drill: با
`pg_restore` جعلی که چیزی برنمی‌گرداند، اسکریپت **رد کرد** و گفت «۶ اختلاف در بازگردانی» — یعنی
in‌کوریِ راستی‌آزمایی، تأیید کورکورانه نیست ✓.

## ۳) P0-3 — مهاجرت پروداکشن = مهاجرت CI

Dockerfile (گام‌های migrator) از `drizzle-kit push --force` به `node scripts/migrate-db.mjs` عوض شد:

```
۱ پشتیبان (fail-closed داخل خود migrate-db) + مهاجرت نسخه‌دار drizzle/*.sql + دفتر مهاجرت
۲ apply-patches.mjs        ۳ seed-base.mjs        ۴ hardening.mjs (RLS)        ۵ verify-backup.mjs
```

و **baseline خودکار** برای دیتابیس‌هایی که قبلاً با push ساخته شده‌اند: اگر جدول‌ها بودند و دفتر مهاجرت
خالی، hashها «اعمال‌شده» ثبت می‌شوند (هیچ SQL اجرا نمی‌شود) تا ارتقای نصب دانشگاه نشکند؛ با
`AFAGH_MIGRATE_NO_AUTO_BASELINE=1` خاموش می‌شود.

**اثبات زنده** — پنج گام، همان ترتیب Dockerfile، روی PostgreSQL 18 واقعی:

```
✅ پشتیبان: afagh-….dump (sha256 …)   ✅ مهاجرت: 13/13 در دفتر ثبت   ✅ پچ‌ها: ۲۲ جدول حیاتی
✅ seed-base                            ✅ RLS: 41/41 · گرنت ستونی · پوشش ۵۴ جدول
✅ verify-backup: آرشیو سالم (۳ جدول · ۲ بخش داده · ۱ قید)
دیتابیس «push‌ساخته» (فقط جدول users) → ⚠ baseline خودکار ثبت ۱۳ مهاجرت، بدون اجرای مجدد SQL ✓
```

نکتهٔ جانبیِ همان مسیر: `pg-hardening.sql` نام دیتابیس `afagh_db` را hardcode داشت
(`GRANT CONNECT ON DATABASE afagh_db`) — هر نصبی با نام دیتابیس دیگر در گام سخت‌سازی می‌شکست.
حالا `__AFAGH_DB__` از `DATABASE_URL` پر می‌شود ✓ (روی `afagh14`/`afagh16` آزموده شد).

## ۴) P0-4 — حساب اولیه بدون رمز ثابت

- **جدید**: `scripts/create-admin.mjs` — رمز تصادفی ۲۴تایی، `mustChangePassword=1`، باطل‌کردن نشست‌های
  قبلی همان کاربر، نقش از روی `roles.code`، نوشتن در فایل credential با `0600`، و چاپ رمز **فقط** با
  `--show` (اجرای دستی). هش با همان الگوی `salt:scrypt` برنامه است (تست شد که با `verifyPassword` جور درمی‌آید).
- `deploy-debian.sh`: اگر حساب `ADMIN` نباشد، انتهای نصب همین اسکریپت را اجرا می‌کند و رمز را در
  `bootstrap-credential.txt` (۰۶۰۰) می‌گذارد؛ **هیچ رمز ثابتی چاپ نمی‌شود**. اگر مدیر بود، دست نمی‌زند
  (`make admin` برای چرخش). خلاصهٔ نصب حالا همین را توضیح می‌دهد و روشن می‌گوید حساب دمو در ایمیج تولید قفل است.
- `seed-base.mjs` هم دیگر در پروداکشن نمی‌گوید «با حساب دمو وارد شوید» → پیام `make admin`.
- قفل دمو از ENV به **build-arg** تبدیل شد: `AFAGH_DEMO_LOCK` (پیش‌فرض `1` = قفل). جعبهٔ نمایشی واقعی:
  `AFAGH_DEMO_LOCK=0` + `AFAGH_DEMO_MODE=1` + `AFAGH_ALLOW_INSECURE_DEMO=true`.

**اثبات زنده روی همان دیتابیس hardening‌شده** (`next dev` + کوکی نشستِ ساخته‌شده در DB):

```
اولین ورود، هر دو مسیر:            HTTP 307 → /change-password            ✓ تغییر رمز اجباری
پس از تکمیل تغییر رمز:             /admin/curriculum HTTP 200 (+ نشانک)، /login 200
کاربر ناشناس:                      HTTP 307 → /login?next=%2Fadmin%2Fcurriculum ✓
create-admin دوباره بدون --reset → exit 4 «از قبل هست»                    ✓
فایل credential: ۰۶۰۰ · هش scrypt مطابق verifyPassword · نقش ADMIN وصل ✓
```

## ۵) دو P1 که در همین دور بسته شد

- **Scheduler** (`deploy/scheduler.sh`): سه طبقه — `ok` / `retryable` (۵xx، timeout، refused → تلاش
  دوباره پس از `SCHEDULER_RETRY_MIN`، پیش‌فرض ۵ دقیقه) / `permanent` (۴۰۱·۴۰۳·۴۰۴·۴۰۵ →
  `[CRITICAL]` و بازگشت به بازهٔ عادی تا درگاه بدپیکربندی کوبیده نشود) + شمارش شکست پیاپی و
  `[ALERT] <label> N بار پشت‌سرهم…` پس از `SCHEDULER_FAIL_ALERT_AT` (۳) برای لاگ‌بردار/الرت.
  با `wget` جعلی در ۴ سناریو آزموده شد: ok → لاگ معمولی؛ 403 → یک `[CRITICAL]` و بدون retry سریع؛
  refused → retry + `[ALERT]` از شکست سوم؛ 500 → retry ✓
- **دروازهٔ ممیزی**: `ci-audit.mjs` با `AFAGH_REQUIRE_REAL_AUDIT=1` قطعی رجیستری = **شکست**؛ job تازهٔ
  CI «دروازهٔ انتشار» (فقط push به main / dispatch) همین را می‌زند + `docker compose config` را با
  سه سکرت قوی **موفق** و با `.env` خالی **رد‌شده** بررسی می‌کند (پیام خطا هم چک می‌شود).

## ۶) دروازهٔ تازه در مخزن: `tests/release-hardening.test.ts` (۱۷ ادعا) — داخل `npm test`

سه نوع بررسی: رفتار CLI سیاست رمز (۷ حالت)، گارد سکرت اپ (شامل واگرایی‌نزدن فهرست ضعیف‌های دو لایه)،
و دروازه‌های استاتیک: نبود `:-` برای رمزها، وجود `:?`، نبود `drizzle-kit push` در دستورهای Dockerfile،
volume پشتیبان، `verify-backup` به‌عنوان گام بعدی، `sha256/LATEST/retention`، نبود چاپ `123456` در
اسکریپت نصب، `chmod 600`، حداقل طول ۲۴، و وجود هدف‌های `verify-backup/drill/admin/copy-backups-to-host`.

## ۷) سند تازه: `docs/RELEASE-READINESS.md`

وضعیت P0/P1، قرارداد پشتیبان/بازیابی، سیاست رمز، جدول گیت‌های CI و **چک‌لیست RC-2**.
هم‌راستا با حکم شما: **فعلاً `v1.0.0` نمی‌زنیم**؛ این دور RC-1 را به «RC-1 + ابزار اثبات‌شده» می‌رساند و
برای RC-2 دو چیز بیرون از مخزن لازم دارد: اجرای واقعی روی VM/سرور دانشگاه و سه سناریوی E2E.

## ۸) وضعیت دروازه‌ها

| دروازه | نتیجه |
|---|---|
| `tsc --noEmit` | ✅ صفر خطا |
| `npm test` (۲۱ مجموعه — `release-hardening` اضافه شد) | ✅ exit 0 · ۱۷/۱۷ |
| `node scripts/audit-actions.mjs` | ✅ ۲۴۳ اکشن، بدون تغییر (هیچ اکشن تازه‌ای ساخته نشد؛ ابزارها `scripts/`‌اند) |
| `npm run build` | ✅ Compiled successfully (گارد سکرت در build فعال نمی‌شود — `NEXT_PHASE`) |
| `bash -n` / YAML parse سه فایل compose / `node --check` پنج اسکریپت | ✅ |
| PostgreSQL واقعی (PG 18): مهاجرت نسخه‌دار + baseline خودکار + hardening + policy + create-admin + retention + verify | ✅ ۱۱+ گام، طبق آزمون‌های بالا |
| `next dev` روی دیتابیس hardening‌شده | ✅ 307→/change-password، 200 رندر، 307 ناشناس |

## ۹) باز مانده (ترتیب پیشنهادی من برای RC-2)

1. **E2E مرورگری** (P1-02): Playwright در سه سناریو — ورود→انتخاب‌واحد→پرداخت / نمره→تأیید مدیر گروه /
   استعلام مدرک. تا این نباشد «Feature Complete» را تائید نمی‌کنم.
2. **حذف اتصال مالک از کانتینر اپ** (P1-01): ۱۲۵ فایل باید از `db` به `appDb`/`withUserRls` مهاجرت کنند؛
   برنامهٔ ۴ تایی: (الف) inventory + ESLint rule که `from '@/db'` را در `src/app/**` ممنوع کند به‌جز
   مسیرهای سروری مجاز، (ب) مهاجرت ماژول‌به‌ماژول، (پ) حذف `DATABASE_URL` از سرویس `app`، (ت) `make drill`.
3. **اجرای واقعی `deploy-debian.sh` روی VM** و بایگانی خروجی پنج گام + `make drill` (P1-05 تمام‌شدنی).
4. Observability: metrics/الرت از خطوط `[ALERT]` زمان‌بند، نسخهٔ انتشار در `/api/health`.
