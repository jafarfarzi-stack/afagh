# دور ۱۳ — مرتب‌سازی لایهٔ اکشن‌های «برنامهٔ درسی»

**کاربر:** «برنامه درسی را مرتب کن» → آخرین تکهٔ یکپارچهٔ این ماژول باز شد:
`src/app/admin/curriculum/actions.ts` با **۱۱۸۰ خط** و **۲۵ اکشن** + ۱۰ هلپر DB + همهٔ typeها در یک فایل.
مثل دورهای ۱۰ تا ۱۲: **فقط جابه‌جایی، بدون هیچ تغییر رفتاری** (هر خط کد عیناً منتقل شد).

---

## ۱) چیدمان تازه

```
src/app/admin/curriculum/actions/
├── shared.ts     ۳۴۴ خط   لایهٔ مشترک — بدون 'use server'
│                  · type Act<T> و ۱۷ type ورودی/خروجی
│                  · EDITORS / APPROVERS / PHASE
│                  · getVersionOrThrow · assertEditable · requireActorStaff
│                  · assertUniqueVersionCode · loadVersionData · buildCheckInput
│                  · runChecks · deepCopyCourseData · revalidateCurriculumPaths · DEFAULT_MIN_ROLES
├── read.ts       ۱۱۴ خط    ۴ اکشن  — نمای کلی، جزئیات نسخه، بانک دروس، گروه‌ها
├── courses.ts    ۳۵۹ خط    ۹ اکشن  — درس بانک، افزودن/حذف/ویرایش، ترم‌بندی، هم‌گردانی نقش، شرط فارغ‌التحصیلی
├── versions.ts   ۱۷۳ خط    ۳ اکشن  — ایجاد نسخه، ویرایش متادیتا، بازنگری R+1
├── rules.ts      ۱۰۹ خط    ۳ اکشن  — پیش‌نیاز / هم‌نیاز / کف نمره
└── lifecycle.ts  ۲۳۱ خط    ۶ اکشن  — اعتبارسنجی، ارسال، تأیید، رد، انتشار، بایگانی
```

جمعاً ۱۳۳۰ خط در ۶ فایل به‌جای ۱۱۸۰ خط در ۱ فایل (اختلاف = سرصفحهٔ توضیحی و importهای هر فایل).
بزرگ‌ترین فایل الان **۳۵۹ خط** است (قبلاً ۱۱۸۰).

**دو اصلاح سازمانی که لابه‌لای همین جداسازی انجام شد:**
- `createCourseBankAction` زیر بنر «خواندن‌ها» گم شده بود (اشتباه دورهای قبل) → به `courses.ts` رفت؛ حالا بنرها با محتوا می‌خواند.
- `bulkAssignSemestersAction` نگه داشته شد با یادداشت همان ردیف سند طراحی (`docs/design/CURRICULUM-SCHEDULING-DESIGN-V1.md:372`)؛ هنوز هیچ UI‌ای صدایش نمی‌کند و فقط اکشن است.

## ۲) چرا این شکاف، و چرا بدون barrel

- فایل‌های اکشن بر اساس **چرخهٔ حیات نسخه** جدا شدند (خواندن / درس / نسخه / قاعده / حیات)، نه تصادفی؛ هر کدام یک سرصفحهٔ توضیحی دارند که «کجا، با چه گاردی، با چه اثر جانبی‌ای» را می‌گوید.
- **`index.ts` (barrel) عمداً ساخته نشد.** زنجیرهٔ `export … from` از یک فایل بدون `'use server'` رفتار Next در ثبت Server Action را به حالت نامطمئن می‌برد؛ مصرف‌کننده‌ها مستقیم فایل مشخص را import می‌کنند:
  - `CurriculumProvider.tsx` → ۵ خط import (read/versions/courses/rules/lifecycle)
  - `page.tsx` (SSR) → `./actions/read`
- `shared.ts` **عمداً بدون `'use server'` است**: Next هر export یک فایل «use server» را به‌عنوان اکشنِ قابل‌فراخوانی از کلاینت بیرون می‌دهد؛ اگر آن‌جا `'use server'` می‌گذاشتم، ۱۰ هلپر داخلی (مثل `runChecks`) عمومی می‌شدند و ممیز CI هم ۲۵ اکشن جعلی اضافه می‌کرد (در همین دور این را آزمودم: شمار ممیزی از ۲۴۳ به ۲۶۸ رفت و بلافاصله برگردانده شد).

## ۳) اثبات‌ها (همه سبز)

| بررسی | نتیجه |
|---|---|
| `tsc --noEmit` | ✅ خطای صفر |
| `npm test` (۲۰ مجموعه، از جمله `curriculum-core` ۹۳ ادعا و `curriculum-ui-render`) | ✅ exit 0 |
| `node scripts/audit-actions.mjs` | ✅ **۲۴۳** اکشن ممیزی‌شده — دقیقاً همان عدد پیش از تفکیک؛ «همه گارد دارند» |
| `npm run build` | ✅ Compiled successfully (۶ فایل اکشن ثبت شدند) |
| امانت‌داری متن | ✅ **۴۹/۴۹** declaration (۲۵ اکشن + ۱۰ هلپر + ۱۴ type) بایت‌به‌بایت یکسان؛ چندستِ خطوط کد خالص: **۱۰۴۶ = ۱۰۴۶، گم‌شده ۰، اضافه ۰** |
| گارد در همان بدنه | ✅ ۲۵/۲۵ اکشن همچنان `requireRole(...)` را در بدنهٔ خودشان می‌زنند (نه از هلپرِ هم‌فایل) → دروازهٔ CI تنها با جابه‌جایی سبز می‌ماند |
| importهای بی‌استفاده | ✅ صفر در هر ۶ فایل |
| رندر زنده روی PostgreSQL واقعی | ✅ **۸/۸** (پایگاه دادهٔ تازه: ۱۳ مهاجرت + `seed-base` + فیچر: ۴ درس بانک، نسخهٔ ۱۴۰۲ PUBLISHED با `minRoleUnits` و قاعدهٔ PREREQ، نسخهٔ ۱۴۰۳ DRAFT با درسِ بدون ترم) |

خروجی دودِ زنده (`/home/user/tmp-pg/smoke13.mjs` روی `next dev` با DB واقعی):

```
✅ نمای کلی (CATALOG)                    [HTTP 200]  «🗂️ نسخه‌های این رشته» + «کد نسخه»
✅ نسخهٔ پیش‌نویس — هدر + ترم‌بندی         [HTTP 200]  «برنامهٔ کارشناسی ۱۴۰۳» + «نامشخص»
✅ پیش‌بارگذاری SSR (بدون Spinner)       [HTTP 200]  «مبانی برنامه‌سازی»
✅ تب اعتبارسنجی + سهم نقش               [HTTP 200]  «🎯 سهم واحد هر نقش»
✅ تب ترم‌ها                              [HTTP 200]  «تابستان»
✅ تب انتقال/بازنگری                     [HTTP 200]  «نسخهٔ جدید (R+1) از همین نسخه»
✅ نسخهٔ منتشرشده (فقط‌خواندنی)           [HTTP 200]  «برنامهٔ کارشناسی ۱۴۰۲»
✅ کاربر ناشناس                           HTTP 307 → /login?next=%2Fadmin%2Fcurriculum
```

یعنی مسیرهای داده‌ای که از فایل‌های تازه می‌گذرند در حالت واقعی آزموده شدند:
`getCurriculumOverviewAction` (نمای کلی)، `getCurriculumVersionDetailAction` (هم client هم پیش‌بارگذاری SSR با `?version=`)،
`resolveTab` روی تب‌های VERIFY/SEMESTERS/TRANSFER، و گارد `requireRole(EDITORS)` در هر دو حالت.

اکشن‌های نوشتنی به‌همان دلیلِ دورهای قبل با HTTP آزموده نشدند (مودال‌ها از URL قابل‌دستیابی نیستند): اثبات آن‌ها
«انتقال بایت‌به‌بایت + build + دروازهٔ ممیزی» است.

## ۴) سند طراحی هم هم‌راستا شد

دو ارجاع کهنه به `actions.ts` اصلاح شد تا با چیدمان تازه بخواند:
`docs/design/CURRICULUM-SCHEDULING-DESIGN-V1.md` (ردیف فاز ۳) و `docs/design/SCHEDULING-CHAIN-AUDIT.md` (ردیف Lifecycle) →
`admin/curriculum/actions/` با «۲۵ اکشن در ۵ فایل + لایهٔ مشترک `shared.ts`».

## ۵) وضعیت ماژول پس از این دور

| فایل | خط |
|---|---|
| `actions/*` (۶ فایل) | ۱۳۳۰ (حداکثر ۳۵۹) |
| `CurriculumProvider.tsx` | ۶۰۳ |
| `curriculum-core.ts` / `types.ts` / `page.tsx` / `CurriculumManagerClient.tsx` | ۲۰۶ / ۷۴ / ۹۷ / ۳۱ |
| `components/*` | ۱۷ فایل (حداکثر ~۲۰۰ خط) |

هیچ فایل دیگری در مخزن از `admin/curriculum/actions` import نداشت، پس سطح API بیرونی ماژول بدون تغییر ماند.

## ۶) leftovers (اگر دور بعدی خواسته شد)

1. `StudentsManagerClient.tsx` (۱۲۵۹ خط) — تنها یکپارچهٔ بزرگ باقی‌مانده؛ شکستن آن را عمداً مکانیکی نمی‌کنم،
   چون چهار بلوک `stuTab === 'info_combined'` در هم تنیده‌اند و اول باید حالت آن تب به یک `useReducer` منتقل شود.
2. تست یکپارچگی `handleSaveRoleTargets` (دو اکشن پشت‌سرهم: `updateCurriculumMetaAction` سپس `validateCurriculumAction`)
   روی DB واقعی — با همین اسکریپت دود قابل اضافه کردن است.
3. تست SQL برای `getTranscript` (دور ۱۱ موکول شد؛ `server-only` زیر `tsx` اجازهٔ import زنجیرهٔ اکشن را نمی‌دهد،
   پس باید مستقیماً روی کوئری نوشت).
