# گزارش رفع ریسک‌ها — بازبینی چهارم (اقدامات درخواستی بازبین + سبز شدن CI واقعی)

تاریخ: ۱۴۰۵/۰۶/۱۳ — نسخهٔ نهایی تأییدشده: `cd08819` (main / Next 16) — CI واقعی گیت‌هاب: **۳/۳ job سبز** (هر گام اجرا و پاس شد)

## سه مورد درخواستی بازبین — همه رفع و اثبات شد

### ۱) 🔴 High — fail-open باقی‌مانده در `assertServerActionOrigin` ✅
`catch { return { ok: true }; }` → **fail-closed**:

```ts
} catch (err) {
  console.error('[csrf] assertServerActionOrigin failed:', err);
  return { ok: false, error: 'خطا در بررسی امنیتی درخواست؛ دوباره تلاش کنید.' };
}
```
هر خطای غیرمنتظره در «بررسی منشأ» → **رد درخواست** (رد اشتباه ارزان‌تر از پذیرش CSRF است).

### ۲) 🟠 مهم‌ترین: IDOR در عملیات مالیِ مبتنی بر `id` ✅
در **۹ اکشن**، پس از یافتن رکورد با `id`، حالا `requireStudentScope(row.studentId)` اجرا می‌شود (قبل از هر UPDATE/DELETE):

| اکشن | رکورد | اکشن | رکورد |
|---|---|---|---|
| `setDiscountStatusAction` | student_discounts | `clearChequeAction` | payment_cheques |
| `deleteDiscountAction` | student_discounts | `setChequeStatusAction` | payment_cheques |
| `setSponsorshipStatusAction` | student_sponsorships | `deleteChequeAction` | payment_cheques |
| `deleteSponsorshipAction` | student_sponsorships | `setLoanStatusAction` | student_loans |
| `deleteLoanAction` | student_loans | | |

نتیجه: داشتن ID رکورد مالی دانشجوی دیگر، بدون مجوز scope، **هیچ اثری ندارد** (حتی با Role مالی).

### ۳) 🟡 CI — حالا نتیجهٔ واقعی قابل اثبات است ✅
`get_commit_combined_status` فقط state های قدیمی (commit status) را نشان می‌دهد؛ **Actions از مسیر `/check-runs` و `/actions/runs` قابل مشاهده است**. نتیجهٔ اجرای واقعی برای `cd08819`:

```
▸ کالبد  — تایپ، تست و بیلد     → success  (tsc، npm test ۹۸/۹۸، audit-actions ۱۴۳/۱۴۳، build، ci-audit)
▸ RLS    — اثبات جداسازی سطری  → success  (drizzle push → apply-patches → seed → hardening →
                                                    rls-test ۲۲/۲۲ → concurrency-test ۴/۴)
▸ فاز صفر — سلامت کد            → success  (node --check همهٔ فایل‌ها)
```

## چه چیزهایی در مسیر سبز شدن CI پیدا و رفع شد (کد آسیب‌پذیر/باگ واقعی)

| یافته | شدت | رفع |
|---|---|---|
| `drizzle-orm 0.36.4` — **SQL Injection از طریق escaped identifiers** (GHSA-gpj5-g38j-94v9) | 🔴 High | ↑ `0.45.2` + `drizzle-kit 0.31.10` (پس از ارتقا: ۴ moderate، صفر High) |
| `majors.majorCode` بدون constraint یکتا؛ seed با `ON CONFLICT` می‌شکست (شکست CI از ۲ دور قبل!) | 🟠 | `.unique()` در schema.ts |
| **۲۱ جدول هویتی/مالی بدون RLS** (payment_cheques، student_discounts، student_loans، student_sponsorships، user_roles، staff_roles، …) | 🟠 High (عدم پوشش دفاعی) | بخش ⑧ در pg-hardening.sql: ENABLE RLS + پالیسی self-read (الگوی موجود)؛ legacy_code_maps → deny-all صریح. سینتکس با Postgres واقعی (WASM/PGlite) راستی‌آزمایی شد: ۲۱/۲۱ |
| `npm audit` با ۵۰۳ رجیستری → CI قرمزِ کاذب | 🟡 | `scripts/ci-audit.mjs`: ۳ تلاش + backoff؛ شکست فقط برای یافتهٔ واقعی high/critical؛ خرابی endpoint → هشدار مستند؛ هر خطای دیگر fail-closed |
| rls-test — **هرگز در CI پاس نشده بود**؛ ۴ باگ نهفته | 🟡 | categoryId (NOT NULL)، `set_config` بدون SELECT، چند دستور+پارامتر (بازشدن به BEGIN/استیت/COMMIT)، تراکنش abort بدون ROLLBACK، پذیرش REVOKE→42501، تراکنش تازه برای هر چک deny-all |
| concurrency-test — FK `discountTypeId` بدون ردیف (seed نمی‌سازد) | 🟡 | ساخت نوع تخفیف در خود تست |

## وضعیت درخواست‌های بازبین

- ✅ High (fail-open CSRF) — رفع شد
- ✅ مهم‌ترین مورد (IDOR مالی id-based) — ۹/۹ اکشن مسدود شد
- ✅ CI — اجرای واقعی ۳/۳ سبز با تمام گام‌ها (تصمیم بازبین روی commit نهایی قابل استناد است)
- امتیاز بازبین 8.7 → اکنون: Critical صفر، High صفر (در خط main/Next16)؛ 🟡 باقی‌ماندهٔ شناخته‌شده فقط moderateهای زنجیرهٔ minio (در آستانهٔ پایین threshold، نیازمند ارتقای breaking) و خط Next14 که legacy است (next 14.2.35 در بازهٔ مشاوره‌های next — توصیه: تولید فقط روی Next16)

## گام بعدی (برنامهٔ بازبین)
ممیزی کامل API Routeها + Server Actionها + نمره/امتحان (FINALIZE نمره سمت کلاینت) برای IDOR / escape / race → جدول نهایی Critical/High/Medium/Low با فایل/خط/راه‌حل.
