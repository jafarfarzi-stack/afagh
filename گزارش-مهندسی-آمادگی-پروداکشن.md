# 🔍 گزارش مهندسی جامع — سامانه جامع آموزشی دانشگاه آفاق (afagh-next)

> **شاخه:** `arena/01a05c13-afagh` — **تاریخ بررسی:** ۱۴۰۵/۰۶/۱۲
> **روش:** بررسی مستقیم کد (نه README) — ~۹۰ فایل در `src/`، Docker/استقرار، CI، اسکیمای ۷۳ جدولی، ۱۸ مسیر API و ۲۶ فایل Server Action
> **نتیجهٔ نهایی:** ✅ ایده و معماری :: بسیار خوب — ❌ **آمادهٔ استقرار واقعی دانشگاه: خیر (مشروط)** — ۲ یافتهٔ Critical جدید (XSS سمت ذخیره + CSRF)، ۲ یافتهٔ P0 تأییدشده از بررسی قبلی

---

## ۰) امتیاز آمادگی پروداکشن (Production Readiness Score)

| حوزه | امتیاز | خلاصه |
|---|---|---|
| معماری کلی | **۸.۵/۱۰** | تفکیک Engine/Lib/App، رویدادمحوری، داده‌محوری قوانین |
| داده و مدل | **۷.۵/۱۰** | ۷۳ جدول منسجم؛ اما auto-healing runtime مشکل‌ساز است |
| احراز هویت و نشست | **۶.۵/۱۰** | scrypt + token ۳۲ بایتی خوب؛ بدون rate-limit و بدون چرخش نشست |
| RBAC / RLS | **۵/۱۰** | طراحی RLS عالی، **اجرا ناسازگار** — fallback به اتصال مالک (P0) |
| API و Server Actions | **۶/۱۰** | گیت‌ها تقریباً سر جاست؛ **CSRF در route handlerها** و نبود Origin-check |
| امنیت (upload/headers/whitelist) | **۴/۱۰** | 🔴 Stored XSS از طریق MIME کنترل‌شده توسط کلاینت |
| فرانت‌اند | **۷/۱۰** | سه داشبورد ایزوله و UX خوب؛ no-CSP در حالت غیر Caddy |
| استقرار / DevOps | **۶.۵/۱۰** | Multi-stage + non-root + Caddy؛ جدا نبودن dev/prod compose و رمزهای پیش‌فرض |
| تست | **۲.۵/۱۰** | فقط ۲ تست rule؛ **بدون تست موتورها/API/E2E اتوماتیک**؛ تست در CI اجرا نمی‌شود |
| کارایی | **۶.۵/۱۰** | ایندکس‌ها و BI snapshots خوب؛ N+1 در پویش فارغ‌التحصیلی |
| نگهداری | **۶.۵/۱۰** | ۷۳ جدول + چند «همانند» ناسازگار (EQUIV_UNITS، cron secrets) |
| مانیتورینگ/بکاپ/DR | **۵/۱۰** | `make backup` وجود دارد؛ لاگ فقط console؛ بدون alerting |

**وزن‌دار: ~۶.۱/۱۰ → «مشروط» — با رفع ۶ مورد P0/P1 قابل قبول است.**

---

## ۱) راستی‌آزمایی یافته‌های بررسی قبلی

### 🔴 P0-1 — Fallback به اتصال مالک پس از خطای RLS — **تأیید شد** ✅
`src/db/index.ts` (تابع `withUserRls`، ~خط ۱۴۰–۱۷۰):
```ts
if (rlsInfraProblem) {
  return (db as any).transaction(...)   // ← اتصال مالک (BYPASSRLS)
}
```
دقیقاً «RLS شکست خورد → از اتصال امن خارج شو». **اصلاح پیشنهادی:** در `NODE_ENV=production` حذف fallback (throw + لاگ + هشدار)، و در توسعه صرفاً لاگ. + فعال‌سازی `FORCE ROW LEVEL SECURITY` روی ۴ جدول حساس (enrollments, cart_items, notifications, student_requests) تا حتی مالک هم نتواند دور بزند.

### 🔴 P0-2 — Credentialهای پیش‌فرض — **تأیید با اصلاح جزئی** ✅/🟡
- ✅ مشکل واقعی: `docker-compose.yml` → `POSTGRES_PASSWORD: ${...:-afagh}` و `MINIO_ROOT_PASSWORD: ${...:-afagh-secret}` و `pg-hardening.sql` → `CREATE ROLE afagh_app ... PASSWORD 'afagh_app'` (رمز در فایل ریپو).
- 🟡 **اصلاح بر ادعای شما:** پورت‌های PostgreSQL/Redis/MinIO **به `127.0.0.1` باند شده‌اند** (`ports: ['127.0.0.1:5432:5432']`) — یعنی از خارج سرور در دسترس نیستند. این خوب است؛ ولی باز هم بهتر: **حذف publish پورت‌ها** در compose پروداکشن + شبکهٔ داخلی `internal: true` برای DB/Redis/MinIO.
- ✅ `.env.prod.example` جایگزین‌های `CHANGE_ME_STRONG_PASSWORD` دارد (خوب)، ولی `deploy-debian.sh` خودش رمز تصادفی می‌سازد (خوب) — مشکل اصلی، مسیرهای پیکربندی ساده (install/simple compose) است که پیش‌فرض ضعیف را نگه می‌دارند.

### 🔴 P0-3 — حساب‌های Demo در auth — **تأیید شد + یافتهٔ جانبی مهم** ✅
- `src/lib/auth.ts` → `ensureDemoUser()`: رمز را به `123456` **برمی‌گرداند** اگر کاربر آن را عوض کرده باشد:
```ts
const isPassValid = await verifyPassword('123456', u.passwordHash);
if (!isPassValid || !u.isActive) { ... set 123456 }
```
- 🔴 **یافتهٔ جانبی (باگ کارکردی + امنیتی):** این رفتار **با حلقهٔ mustChangePassword در تضاد است** — کاربر دمو رمز را عوض می‌کند، ورود بعدی دوباره `123456` می‌شود و فلگ `mustChangePassword` هم مقداردهی نمی‌شود. یعنی «تغییر اجباری رمز» عملاً برای حساب‌های دمو دور زدنی/بی‌اثر است.
- **اصلاح:** پرچم `DEMO_MODE` (پیش‌فرض `false` در prod): در production، `ensureDemoUser`/`DEMO_ACCOUNTS` کل غیرفعال یا فقط فقط در `DEMO_MODE=true`؛ در توسعه، ریست رمز فقط وقتی `mustChangePassword=0` و در حالت demo باشد.

### 🟠 P1-5 — Middleware بررسی فقط کوکی — **تأیید شد** ✅
`src/middleware.ts`: فقط `req.cookies.get('token')`. قابل دفاع است (چک نهایی در layoutها) — **اما شرطش:** هیچ route handler یا server action نباید بدون گیت بماند. نتیجهٔ ممیزی کامل من در بخش ۲: ✅ تقریباً تمام ۲۶ فایل action گیت دارند؛ نقاط استثنا: `id/actions.ts` و `verify-certificate/actions.ts` که **عمداً عمومی‌اند** (مستند شده) ولی **rate-limit ندارند** → 🟡.

### 🟠 P1-6 — Auto-Healing Schema در runtime — **تأیید شد** ✅
`src/db/index.ts` → `ensureDbSchemaPatches()` با ۴۰+ دستور `ALTER TABLE/CREATE INDEX` در اولین درخواست. برای پروژه‌ای که از قبل `migrator` (Docker) دارد، **باید پچ‌ها به migrator منتقل شوند**: `migrator → drizzle-kit push → seed-base → patches(idempotent) → hardening → app canary`. در runtime فقط `ADD COLUMN IF NOT EXISTS`های اضطراری (و ترجیحاً هیچ) باقی بماند.

---

## ۲) یافته‌های جدید این بررسی (Critical و مهم)

### 🔴 C-1 — Stored XSS از طریق آپلود اسناد (MIME کنترل‌شده توسط کلاینت)
**مسیر:** `src/app/api/admin/archive/upload/route.ts` + `src/app/api/archive/[docId]/route.ts`
- آپلود: `mimeType: file.type` **مستقیم از کلاینت** ذخیره می‌شود؛ پسوند هم فقط به `[a-z0-9]` محدود می‌شود (هر چیزی مجاز است) و هیچ whitelist مایم وجود ندارد.
- سرو: `new NextResponse(fileBuffer, { headers: { 'Content-Type': doc.mimeType } })` → فایل با `text/html` یا `image/svg+xml` از **همان ارجین** سرو می‌شود.
- نتیجه: دانشجو یک `file.html`/`file.svg` حاوی `<script>` با `type: text/html` آپلود می‌کند → با کلیک روی «مشاهده» یا باز شدن لینک `/api/archive/123`، **اسکریپت روی ارجین سامانه اجرا می‌شود** (دزدی نشست، درخواست‌های سمت کاربر).
- ⚠️ در حالت HTTPS (Caddy) هدر `X-Content-Type-Options: nosniff` ست می‌شود — اما SVG/HTML با `text/html` تحت nosniff **همچنان رندر می‌شوند**؛ یعنی حتی با Caddy آسیب‌پذیر است.
- **اصلاح:** ① whitelist: `image/jpeg|png|pdf` فقط؛ ② `Content-Disposition: attachment` + `X-Content-Type-Options: nosniff` در خود route؛ ③ در صورت نیاز SVG، سرو با `Content-Security-Policy: sandbox; default-src 'none'`.

### 🔴 C-2 — CSRF در Route Handlerها (در حالت SameSite=None)
- در HTTPS (یا iframe/e2b)، `sessionCookieOptions` → `SameSite=None + Secure` ⇒ کوکی روی درخواست‌های cross-site **هم** ارسال می‌شود.
- Server Actions توسط خود Next.js Origin-check دارند (محافظت دارند ✅)، **ولی Route handlerها** (upload، migration/import، migration/commit، redis-warmup، cronهای POST) از `getSessionUser()` روی کوکی استفاده می‌کنند و **هیچ بررسی Origin/Referer ندارند**.
- حمله: صفحهٔ مخرب → `fetch('https://afagh.../api/admin/migration/import', {method:'POST', credentials:'include', body: form})` → با نشست ادمین قربانی، عملیات حساس انجام می‌شود.
- **اصلاح:** یک `assertSameOrigin(req)` در `src/lib/migration/http.ts` و استفاده در همهٔ routeهای حساس (یا global check در middleware برای `/api/*` با متدهای state-changing).

### 🔴 C-3 — باگ تضاد Demo-Reset با mustChangePassword
شرح در بخش P0-3. **به‌علاوه:** `ensureBaseReferenceData()` و `ensureDbSchemaPatches()` در مسیر لاگین اجرا می‌شوند — DDOS کوچک با درخواست لاگین بالا: هر `login()` با کد موجود در DEMO_ACCOUNTS در صورت نبود رکورد، ده‌ها `ALTER TABLE`/`INSERT` اجرا می‌کند. (کم‌اهمیت ولی برای لاگین‌های همزمان ازدحام +جدول‌های قفل می‌شوند.)

### 🟡 M-1 — نشت‌های اطلاعاتی و نبود Rate-Limit در مسیرهای عمومی
- `src/app/id/actions.ts` (عمومی): هر کد دانشجویی → `{ token, fullName }` بدون هیچ limit — یعنی **شمارش معکوس کدهای دانشجویی** و استعلام نام دانشجویان برای هر کسی.
- `src/app/verify-certificate/actions.ts` و `src/app/verify/[code]/page.tsx` (عمومی): استعلام اصالت — خروجی شامل `nationalId` است؛ بدون rate-limit → brute-force احتمالی.
- `login()` در `auth.ts`: **هیچ محدودیت تلاش/قفل‌شدن ندارد** — تنها دفاع، هزینهٔ scrypt است (~۴۳ms هر تلاش). پیشنهاد: شمارندهٔ تلاش + قفل موقت + alert (الگوی بانکی همان‌جا در OTP نمرات به‌کار رفته).
- **اصلاح:** rate-limit ساده در Redis (یا таблица در PostgreSQL): ۵ تلاش/۱۰ دقیقه برای login و استعلام‌های عمومی + حذف `nationalId` از خروجی استعلام عمومی.

### 🟡 M-2 — Cron Secrets خالی در دیپلوی پیش‌فرض
`docker-compose.yml`: `FINANCE_CRON_SECRET/GARD_CRON_SECRET` پیش‌فرض خالی ⇒ `scheduler.sh` هیچ پویشی صدا نمی‌زند (**یادآوری چک، پویش فارغ‌التحصیلی، BI، گردش کار خاموش می‌مانند**) — فقط پیام هشدار می‌دهد. `deploy-debian.sh` آن‌ها را می‌سازد ولی مسیرهای نصب ساده نه. → حداقل در `install.sh`/README اجباری شود + `restart: always` و placeholderهای CHANGE_ME.

### 🟡 M-3 — اشتراک اشتباه secret و سواپ‌های نسخه‌ای
- `bi-refresh` از `GRAD_CRON_SECRET` استفاده می‌کند (کپی). بهتر: `BI_CRON_SECRET`.
- `EQUIV_SEMESTER_UNITS = 20` (graduation-engine) در برابر `EQUIV_TERM_UNITS` (enroll-engine): دو ثابت همانند، بدون import مشترک → باگ ناسازگاری آینده.

### 🟡 M-4 — نشست‌ها: بدون پاکسازی و بدون چرخش
- `sessions` فقط `expiresAt` دارد؛ جدول هرگز تمیز نمی‌شود (رشد نامحدود).
- token به‌صورت plaintext در DB؛ تغییر رمز/نقش، نشست‌های قدیمی را باطل نمی‌کند.
- **اصلاح:** هش کردن token در DB + `touch` پاکسازی در cron + حذف نشست‌های کاربر هنگام changePassword (کافی: `DELETE FROM sessions WHERE userId=`).

### 🟡 M-5 — نبود Security Headers در بدنهٔ Next
`next.config.mjs` هیچ `headers()` ندارد؛ هدرها فقط در `caddy/Caddyfile` ست می‌شوند (HSTS, nosniff, XFO SAMEORIGIN). در حالت اجرای مستقیم (next start / preview) هیچ‌کدام نیست. → اضافه شدن در next.config + CSP سطح‌بندی‌شده (آن‌جا که CDN/iframe لازم است).

### 🟡 M-6 — نبود تست اتوماتیک و ناسازگاری نسخهٔ Node در CI
- `tests/` فقط ۲ فایل (tuition-rules, finance-rules)؛ **موتورهای انتخابی واحد، نمرات، فارغ‌التحصیلی، امتحانات، BPM به‌کلی بدون تست** هستند؛ ادعاهای E2E در README «دستی»اند.
- `.github/workflows/ci.yml`: `typecheck + build` اجرا می‌شود ولی **`npm test` فراخوانی نمی‌شود**؛ ضمناً `npm test` به `--experimental-strip-types` (Node 22.6+) نیاز دارد در حالی که CI از `node-version: 20` استفاده می‌کند — یعنی تست در CI **می‌شکند**.
- **اصلاح:** Node 22 در CI + اجرای `npm test` + `vitest`/`node --test` برای موتورهای خالص (بدون DB) + smoke تست API با compose (`make ci`).

### 🟡 M-7 — نشت جزئیات خطا به کلاینت
تقریباً همهٔ actions: `fail(e) → error: e.message` — خطاهای داخلی DB/زیرساخت مستقیم به UI می‌رود. → در prod، message خام فقط در لاگ؛ به UI کد/پیام عمومی.

### 🟢 M-8 — مسیرهای حساس بایگانی — تقریباً امن (تأیید مثبت)
- `archiveKey` سمت سرور ساخته می‌شود (`archive/{studentId}/{typeId}-{ts}-{rand}`) → **بدون path traversal از ورودی کاربر** ✅
- `[docId]` بررسی مالکیت + نقش privileged دارد ✅ — فقط کافی است `fileUrl` هم در برابر `../` اعتبارسنجی شود (خطوط `path.join` با `doc.fileUrl` از DB؛ اگر دادهٔ legacy/Migration آلوده باشد → خواندن فایل دلخواه). پیشنهاد: `path.resolve` + `startsWith(UPLOAD_ROOT)`.

---

## ۳) ماتریس اولویت‌بندی اصلاحات

| ID | یافته | شدت | فایل | اصلاح | حجم |
|---|---|---|---|---|---|
| P0-1 | Fallback RLS → مالک | 🔴 Critical | `src/db/index.ts` | fail-closed در prod + FORCE RLS | ~۱h |
| C-1 | Stored XSS (MIME) | 🔴 Critical | `upload/route.ts`, `archive/[docId]/route.ts` | whitelist + attachment + CSP sandbox | ~۱h |
| C-2 | CSRF در routes | 🔴 Critical | `src/lib/migration/http.ts` + همهٔ routes | `assertSameOrigin` سراسری | ~۲h |
| P0-2 | Credentialهای پیش‌فرض | 🔴 P0 | compose, pg-hardening.sql | رهگیری Secret + `internal: true` + حذف PUBLISH | ~۲h |
| P0-3 | Demo + تضاد with mustChange | 🔴 P0 | `auth.ts` | `DEMO_MODE` + رفع تضاد ریست | ~۱.۵h |
| P1-5 | نبود rate-limit (login/استعلامات) | 🟠 P1 | `auth.ts`, `verification.ts`, `id/actions.ts` | شمارنده/قفل Redis + حذف nationalId | ~۲h |
| P1-6 | Auto-healing runtime | 🟠 P1 | `db/index.ts`, `Dockerfile` | انتقال به migrator | ~۱h |
| C-3 | Scheduler secrets خالی | 🟠 P1 | compose/install | اجباری‌سازی + تدوین | ~۰.۵h |
| M-1..M-7 | موارد 🟡 | 🟡 P2 | چند فایل | طبق بخش ۲ | ~۱ روز |

---

## ۴) نقاط قوت تأییدشده (نباید دست‌بخورد)

- ✅ **موتور آیین‌نامه‌ها و minUnits/maxUnits داده‌محور** — هیچ عدد آموزشی هاردکد نیست؛ این بالاترین ارزش معماری پروژه است.
- ✅ **GpaAccumulator** (مقیاس صحیح برای جلوگیری از خطای ممیز شناور) + `canonical()` برای هش قطعی JSON.
- ✅ **چرخهٔ زندگی نمره** (PENDING→…→FINALIZED) با OTP، امضای SHA-256 و تطبیق امضا؛ تفکیک hard error/soft error + کارتابل BPM.
- ✅ **appDb / afagh_app با `NOSUPERUSER NOBYPASSRLS`** — پایهٔ RLS درست است (مشکل فقط ناسازگاری استفاده و fallback است).
- ✅ **Docker multi-stage + کاربر غیر root + healthcheck + migration از SQLite + MinIO + Redis waiting room با Lua اتمیک.**
- ✅ **Audit زنجیره‌ای** (sha256 + prevHash) و واترمارک اسناد + RBAC پوشه‌ای.
- ✅ **CI در حال حاضر**: typecheck + build هر دو پروژه (دقیقاً به‌خاطر همین «خوب ولی ناکافی»).

---

## ۵) جمع‌بندی و توصیه

**پروژه برای «استقرار نمایشی/دمو» کاملاً آماده است؛ برای «استقرار واقعی دانشگاهی» باید ۶ مورد P0/Critical رفع شود (مجموع ~۸ ساعت کار)، سپس:**

1. **مرحلهٔ ۱ (امنیت):** P0-1، C-1، C-2، P0-2، P0-3 ← بعد از این، انجام pentest سبک.
2. **مرحلهٔ ۲ (قابلیت اطمینان):** P1-6 (migration pipeline)، C-3 (cron secrets)، M-4 (نشست‌ها)، بکاپ‌گیری خودکار روزانه + restore drill.
3. **مرحلهٔ ۳ (کیفیت):** تست موتورهای کلیدی با vitest + اجرای تست در CI (Node 22) + smoke E2E با Playwright روی compose.

**پاسخ صریح به «آیا آماده است؟»:** خیر — ولی فاصلهٔ این پروژه تا «آماده» **شش اصلاح تمرکزی** است، نه بازسازی. معماری و مدل داده نیازی به تغییر ندارند.
