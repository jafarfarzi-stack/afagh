# گزارش مقایسهٔ کد کلون‌شده (`arena/01a05c13-afagh`) با شاخهٔ کاری فعلی

تاریخ تهیه: ۱۴ شهریور ۱۴۰۵ (2026-09-05)

| مورد | شاخهٔ کاری فعلی | شاخهٔ کلون‌شده |
|---|---|---|
| نام شاخه | `arena/01a07109-afagh` (از `main`) | `arena/01a05c13-afagh` |
| آخرین کامیت | `6840802` — Merge PR #8 (از `arena/01a07092`) | `e8b73c8` — «جمعبندی امروز + ادامهٔ طراحی سند» |
| تاریخ آخرین کامیت | 2026-09-05 12:03 | 2026-09-04 11:06 |
| تعداد کامیت در تاریخچه | ۱ (تاریخچهٔ squash‌شده) | ۱۴۴ |
| مسیر روی دیسک | `/home/user/afagh` | `/home/user/afagh-01a05c13` |

> ⚠ **این دو تاریخچه هیچ merge-base مشترکی ندارند** (`git merge-base` خالی برمی‌گردد). `main` در مقطعی reset/squash شده و تاریخچهٔ قدیمی را از دست داده است. بنابراین هر `merge` نیازمند `--allow-unrelated-histories` است و عملاً به تعارض گسترده می‌انجامد.

---

## ۱) خلاصهٔ حکم

**شاخهٔ کلون‌شده یک اسنپ‌شات قدیمی‌تر است.** تقریباً در همهٔ محورها (وابستگی‌ها، اسکیما، امنیت، رفکتور فرانت، CI، تست) شاخهٔ کاری فعلی جلوتر است.

```
diff HEAD → arena/01a05c13-afagh
132 فایل تغییر، 12,769 افزوده، 47,627 حذف‌شده
```

یعنی اگر شاخهٔ کلون‌شده را روی کد فعلی بنشانیم، حدود **۳۵ هزار خط خالص از کد جدیدتر پاک می‌شود**.

- فایل‌های موجود در فعلی و **غایب** در کلون: **۷۶**
- فایل‌های موجود در کلون و **غایب** در فعلی: **۱** (فقط `CourseCart.tsx` — که در کلون هم توسط هیچ فایلی import نشده، یعنی کد مرده)
- فایل‌های تغییر‌یافته در دو طرف: **۵۵**

---

## ۲) تفاوت‌های کلیدی (به تفکیک محور)

### ۲‑۱. پشتهٔ فناوری — مهم‌ترین شکاف

| بسته | فعلی | کلون‌شده |
|---|---|---|
| `next` | **^16.3.4** | ^14.2.35 |
| `react` / `react-dom` | **^19.2.8** | 18.3.1 |
| `tsconfig.jsx` | `react-jsx` | `preserve` |
| `next.config.mjs` | کلید `eslint` حذف شده (در Next 16 پشتیبانی نمی‌شود) | هنوز `eslint: { ignoreDuringBuilds }` دارد |
| Node در CI | **22** | 20 |
| `package-lock.json` | ~۹۲۰ خط تفاوت | نسخهٔ قدیمی |

مهاجرت Next 14 → 16 و React 18 → 19 روی شاخهٔ فعلی انجام شده؛ برگرداندن کلون یعنی downgrade کامل پشته.

### ۲‑۲. مهاجرت‌های پایگاه‌داده (drizzle)

در شاخهٔ کلون **کل پوشهٔ `afagh-next/drizzle/` وجود ندارد** (در `.gitignore` آن ignore شده است). یعنی این فایل‌ها فقط در فعلی هستند:

- `0000_natural_mandroid.sql` (۱۸۰۴ خط)، `0001_classy_gamora.sql`، `0002_curriculum_versions.sql`، `0003_exam_planning.sql`، `0004_realism.sql`
- `meta/0000_snapshot.json` (۱۱٬۹۵۳ خط)، `meta/0001_snapshot.json` (۱۲٬۴۷۴ خط)، `meta/_journal.json`
- اسکریپت `scripts/migrate-db.mjs` (بکاپ → migrate → verify)

و در نتیجه CI هم متفاوت است:

```diff
- run: AFAGH_BACKUP_DIR="$RUNNER_TEMP/backups" node scripts/migrate-db.mjs   # فعلی: مهاجرت نسخه‌دار
+ run: npx drizzle-kit push --force                                          # کلون: push مخرب
```

### ۲‑۳. اسکیما (`src/db/schema.ts`) — ۲۲۶ خط تفاوت

جدول‌های **فقط در شاخهٔ فعلی** (۱۱ مورد):

`curriculum_versions`, `curriculum_tracks`, `curriculum_courses`, `curriculum_approvals`, `equivalence_clusters`, `exam_calendar_configs`, `scheduling_allocations`, `scheduling_room_grants`, `term_scheduling_states`, `professor_availability_notes`, `signature_otps`

جدول‌های **فقط در شاخهٔ کلون** (۲ مورد، مدل قدیمی که جایگزین شده):

`syllabuses`, `syllabus_courses`

جمع جدول‌ها: فعلی **۱۳۶** — کلون **۱۲۷**.

### ۲‑۴. امنیت / RLS

در `src/db/pg-hardening.sql` و `scripts/hardening.mjs`، شاخهٔ فعلی این موارد را دارد و کلون **ندارد**:

- `curriculum_approvals` در فهرست deny-all (دفتر ممیزی فقط‑مالک)
- سیاست‌های RLS برای `professor_availability_notes` و `signature_otps`
- گیت «پوشش کامل RLS» با این جدول‌های جدید (fail-closed در hardening)

### ۲‑۵. رفکتور فرانت (دور ۸) — بزرگ‌ترین تفاوت حجمی

شاخهٔ فعلی کامپوننت‌های حجیم را شکسته و منطق سرور را به `actions.ts` منتقل کرده؛ در کلون همه‌چیز هنوز داخل یک فایل کلاینت غول‌پیکر است:

| ماژول | فعلی | کلون |
|---|---|---|
| `admin/curriculum` | `page.tsx` + `actions.ts` (۱۰۰۷ خط) + کلاینت سبک | `CurriculumManagerClient.tsx` یکپارچه (+۳۲۹۳ خط) |
| `admin/exams` | `actions.ts` (۳۷۴ خط) جدا | `ExamPlanningClient.tsx` یکپارچه (+۳۱۷۲ خط) |
| `admin/payroll` | ۹ کامپوننت تب + `payrollData.ts` + `payrollReducer.ts` | `PayrollEngineClient.tsx` یکپارچه (+۱۹۸۶ خط) |
| `professor/grades` | ۵ کامپوننت تب + `grades-core.ts` + `gradesReducer.ts` + `types.ts` | `ProfessorGradesClient.tsx` یکپارچه (+۲۰۱۱ خط) |
| `admin/scheduling` | `actions.ts` (۶۵۶ خط) جدا | یکپارچه |
| `open-courses`, `professor/attendance|availability|contract` | `actions.ts` جدا | یکپارچه |

### ۲‑۶. کتابخانه‌های دامنه — فقط در شاخهٔ فعلی

`audit-chain.ts`, `class-session-generator.ts`, `contract-engine.ts`, `curriculum-apply.ts`, `curriculum-resolution.ts`, `curriculum-types.ts`, `curriculum-validator.ts`, `demo-grades-seed.ts`, `exam-actions.ts`, `exam-core.ts`, `exam-engine.ts`, `exam-planning.ts`, `exam-scheduler.ts`, `scheduling-core.ts`, `scheduling-engine.ts`, `scheduling-health.ts`

### ۲‑۷. تست‌ها

| | فعلی | کلون |
|---|---|---|
| `npm test` | ۸ فایل تست | **۲** فایل تست (`tuition-rules`, `finance-rules`) |
| تست‌های غایب در کلون | `grades-reducer`, `exam-core`, `scheduling-core`, `exam-scheduler`, `curriculum-domain`, `curriculum-validator` | — |
| `test:rls` | دارد (`scripts/rls-test.mjs`) | ندارد |
| اسکریپت‌های بار/همزمانی | `exam-load-*`, `load-test-*`, `scheduling-load-*`, `exam-concurrency-test` | ندارد |

### ۲‑۸. قابلیت‌های کاربردی غایب در کلون

- **گارد تداخل امتحان (فاز ۱۰)**: `previewExamConflictsAction`، مودال HARD/SOFT، و `acceptSameDayRisk` در صف Redis (`waitingRoom.ts` + `enroll-engine.processQueuedSubmit`) — در کلون هیچ‌کدام نیست.
- **فاز ۱۱ (مدیر گروه واقعی)**: در `auth.ts` فعلی دو حساب دموی `DEP_HEAD` جدید (`0041111111`، `0042222222`) و اتصال idempotent استاف به گروهِ واقعی (`resolveDepartmentId` / `linkDemoDepHead`) وجود دارد؛ در کلون فقط یک `DEP_HEAD` بدون گروه هست.

### ۲‑۹. استقرار و حافظهٔ بیلد

فقط در شاخهٔ فعلی:

- `scripts/ensure-build-memory.sh` (۱۰۲ خط) + هدف‌های `make mem` / `make swap` / `make build-lowmem`
- `NODE_MAX_OLD_SPACE` پیش‌فرض **۲۰۴۸** (به‌جای ۳۰۷۲ که روی سرور ۲ گیگی OOM می‌داد) + آرگومان `NEXT_BUILD_ARGS` برای `--webpack`
- منطق swap در `deploy-debian.sh` به اسکریپت مشترک منتقل شده و سطح‌بندی هیپ دقیق‌تر است

### ۲‑۱۰. مستندات

فقط در فعلی: `docs/design/CURRICULUM-SCHEDULING-DESIGN-V1.md`, `ENGINEERING-REVIEW-ALL-MODULES.md`, `ERD-TARGET.svg/png`, `SCHEDULING-CHAIN-AUDIT.md`, `گزارش-دور7-…`, `گزارش-دور8-…`, سه گزارش تست ۱۴۰۵ و بخش‌هایی از `INSTALL.md` (۶۸ خط).

---

## ۳) نتیجهٔ اجرای عملی شاخهٔ کلون‌شده

در `/home/user/afagh-01a05c13/afagh-next`:

| مرحله | نتیجه |
|---|---|
| `npm install` | ✅ موفق — ۲۰۹ بسته در ۱۱ ثانیه |
| `npx tsc --noEmit` | ✅ بدون خطا |
| `npm test` | ✅ ۴۶ + ۹۸ تست موفق، ۰ ناموفق (فقط ۲ فایل تست) |
| `next dev -p 8080` | ✅ بالا آمد (Next 14.2.35)، `/login` با کد ۲۰۰ رندر می‌شود |
| ورود واقعی / داشبوردها | ⛔ نیازمند PostgreSQL + Redis است که در این سندباکس نصب نیست (`docker` هم موجود نیست) — صفحاتی که به DB می‌خورند خطا می‌دهند |

---

## ۴) پیشنهاد

1. **merge نکنید.** ادغام کلِ `arena/01a05c13-afagh` روی شاخهٔ فعلی، مهاجرت Next 16/React 19، مهاجرت‌های drizzle، رفکتور دور ۸، تست‌ها و گاردهای امنیتی را برمی‌گرداند.
2. اگر چیزی از آن شاخه لازم دارید، تنها گزینهٔ واقعی `CourseCart.tsx` است (کامپوننت سبد انتخاب واحدِ مستقل، ۷۷۵ خط) که در خودِ کلون هم استفاده نشده — در صورت نیاز می‌توان آن را جداگانه برداشت و با API فعلی تطبیق داد.
3. شاخهٔ کلون‌شده را به‌عنوان مرجع تاریخی نگه دارید؛ ۱۴۴ کامیت آن تاریخچه‌ای است که `main` بعد از squash از دست داده و برای «چرا این تصمیم گرفته شد» ارزشمند است.
