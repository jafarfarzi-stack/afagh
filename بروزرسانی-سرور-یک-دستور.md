# بروزرسانی سرور با یک دستور — شاخه arena/01a05c13-afagh (HEAD: f3f971d)

## لینک‌های bundle (کل تاریخچهٔ شاخه — 2.9MB)
- اصلی: https://n.uguu.se/jqSZeZrt.bundle
- پشتیبان (۷۲ ساعت): https://litter.catbox.moe/4o1mz6.bundle
- SHA256: a0dbdd79e282034e17aea4fcd73c1c6dc0b6f3a8e39bfd46bd2e74fec45ab05f

## یک دستور (در ریشهٔ مخزن سرور)
```bash
curl -fsSL -o /tmp/afagh.bundle https://n.uguu.se/jqSZeZrt.bundle && git fetch /tmp/afagh.bundle "+refs/heads/arena/01a05c13-afagh:refs/heads/arena/01a05c13-afagh" && git checkout -f arena/01a05c13-afagh && git log --oneline -1 && docker compose up -d --build
```
خروجی باید: f3f971d docs: به‌روزرسانی گزارش رفع ریسک‌های باز (M-3 کامل شد)

## پس از اجرا — لاگ‌ها را ببینید
```bash
docker compose logs migrator | tail -25   # «پچ‌ها اعمال شد» + «RLS فعال ۴/۴» + ۳ خط ✓ secrets
docker compose logs scheduler | tail -10   # فراخوانی ۴ پویش بدون هشدار «خالی»
docker compose logs app | grep -E "\[rls\]|CSRF" | tail   # باید خالی باشد
```

## قبل از اجرا — در .env سرور (یک بار)
- AFAGH_APP_DB_PASSWORD=رمز-قوی-جدید   ← حتماً (نقش امن afagh_app)
- AFAGH_DEMO_MODE=0 ، AFAGH_RLS_FALLBACK= خالی
- FINANCE_CRON_SECRET/GRAD_CRON_SECRET/BI_CRON_SECRET: اختیاری — اگر خالی باشند migrator خودش می‌سازد

## نکته
- اگر تغییرات کامیت‌نشدهٔ محلی دارید، اول `git stash` یا کامیت کنید (checkout -f آن‌ها را پاک می‌کند).
- لینک uguu دائمی است؛ در غیر این صورت bundle در workspace (افاق-share/afagh.bundle) موجود است.
