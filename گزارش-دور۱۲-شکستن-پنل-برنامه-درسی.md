# گزارش دور ۱۲ — شکستن مگاکامپوننت برنامهٔ درسی (۱۸۳۱ خط → ۲۲ فایل) + باگ P0 اسکیما

**تاریخ:** ۹ سپتامبر ۲۰۲۶ — **وضعیت: ✅ کامل، با رندر واقعی روی PostgreSQL تأیید و push شده**

گزینهٔ «۱» پایان دور ۱۱: `CurriculumManagerClient.tsx` (۱۸۳۱ خط، ~۳۸ `useState`، ۵ تب، ۵ مودال، بدون هیچ تست).
در این دور همان الگوی دور ۱۰ (Provider + هستهٔ خالص + فایل مستقل هر تب) اجرا شد — و در میانهٔ کار
یک **باگ واقعی و پنهانِ اسکیما** کشف و رفع شد (بخش ۴).

---

## ۱) قبل و بعد

| | قبل | بعد |
|---|---|---|
| `CurriculumManagerClient.tsx` | **۱۸۳۱ خط** (۹ interface، ۲۰+ تابع/مشتق، ۵ تب، ۵ مودال، ویرایشگر سهم نقش، همه محاسبات) | **۳۱ خط** (فقط Provider + Shell) |
| بزرگ‌ترین فایل ماژول | ۱۸۳۱ | **۵۹۹** (`CurriculumProvider.tsx` — تنها جای وضعیت) |
| منطق خالص قابل تست | صفر (همه داخل `useMemo`/closure راکت) | **۲۰۶ خط** `curriculum-core.ts` + **۹۳ assert** |
| تست رندر هر قطعه UI | نداشت | `curriculum-ui-render.test.tsx` — ۱۷ فایل، هرکدام مستقل رندر می‌شود |
| پیوندپذیری | نبود | `?tab=` و `?version=` (با `resolveTab` و اعتبارسنجی سروری) |
| فایل‌های ماژول | ۳ | ۲۲ (جمع ۲۵۳۵ خط با plumbing؛ بدون احتساب `actions.ts`) |

```
src/app/admin/curriculum/
├── page.tsx                    97  (سرور — پارامترهای tab/version + پیش‌خوانی جزئیات)
├── CurriculumManagerClient.tsx 31  ← فقط Provider + Shell
├── types.ts                    74  ← MajorItem/VersionRow/CourseRow/RuleRow/ApprovalRow/CheckRow/VersionDetail/BankCourse/CurriculumTab
├── curriculum-core.ts         206  ← توابع خالص + ثابت‌های نمایشی (بدون React/DB/DOM)
├── curriculum-context.ts       18  ← ظرف context (جدا از Provider، تا تست رندر اکشن‌ها را وارد گراف نکند)
├── CurriculumProvider.tsx     599  ← ۳۸ useState + اکشن‌ها + ۱۲۰ کلید context
├── actions.ts                 1180 (دست‌نخورده)
└── components/
    ├── CurriculumShell.tsx        33   ├── VersionDetailPanel.tsx  31
    ├── CurriculumHeader.tsx      111   ├── DetailHeaderBar.tsx     48
    ├── CurriculumTabsBar.tsx      38   ├── RoleTargetsEditor.tsx   72
    ├── CurriculumToast.tsx        25   ├── CatalogTab.tsx          74
    ├── CoursesTab.tsx            275   ├── SemestersTab.tsx        165
    ├── VerifyTab.tsx             164   ├── TransferTab.tsx          78
    ├── NewVersionModal.tsx        86   ├── NewCourseModal.tsx      103
    ├── CourseRulesModal.tsx       99   ├── AddCourseModal.tsx      158
    └── RejectModal.tsx            47
```

`tests/curriculum-core.test.ts` و `tests/curriculum-ui-render.test.tsx` به انتهای `npm test` اضافه شدند (اکنون ۲۰ سوئیت).

## ۲) هستهٔ خالص: چه چیزی از closure بیرون آمد

تا پیش از این این منطق‌ها داخل تنِ کامپوننت (بعضی داخل `useMemo`، بعضی داخل IIFE رندر) بودند و تست‌ناپذیر:

| تابع خالص | کار | نکته‌ای که تست قفل می‌کند |
|---|---|---|
| `leafCourseCodesOf` / `leafTotalOf` | پیمایش درخت منطقی `LogicNode` | شاخه به اندازهٔ فرزندانش شمرده می‌شود؛ `course: 0` هم برگ است؛ تکراری‌ها حفظ می‌شوند |
| `buildRuleTree(codes, op)` | ساخت درخت پیش‌نیاز/هم‌نیاز | لیست خالی ← `null` (یعنی «بدون قاعده»، نه `{conditions:[]}`) |
| `validateMinGrade(raw, current)` | کف نمرهٔ مودال قواعد | بدون تغییر ← `changed:false` (اکشن صدا زده نمی‌شود)؛ خارج ۰..۲۰ و `NaN` ← همان پیام خطای قبلی |
| `groupBySemester` | تفکیک دروس به ترم / بدون ترم | ترتیب درون هر ترم حفظ می‌شود |
| `semesterUnitTotal` | جمع واحد | واحد `NaN` = صفر (نه آلوده‌کردن جمع) |
| `overflowSemesters` / `termGrid` / `isOverflowSemester` | چارت ترمی + سرریز | ترم ۵ در چارت ۴ ترمه گم نمی‌شود؛ تابستان هرگز تکرار نمی‌شود |
| `semLabel` | برچسب ترم | `null` → «نامشخص»، ۹ → «تابستان»، ۳ → «ترم ۳» (رقم فارسی) |
| `typeSummaryRows` / `unitsByRoleMap` | جمع نوع درس + واحد موجود هر نقش | نقش ناشناخته آخر می‌نشیند (۹۹ در مرتب‌سازی) |
| `facultyNames` / `departmentNames` / `filterMajorsByTree` | فیلتر درختی دانشکده ← گروه ← رشته | یکتا + مرتب با `localeCompare('fa')`؛ بدون فیلتر = همه |
| `filterBankCourses` / `visibleBank` / `allBankSelected` | فیلتر بانک دروس | جستجو در شناسه/کد/عنوان + پیشوند کد؛ سقف ۶۰ ردیف |
| `faNum` / `faDate` / `STATUS_UI` / `ROLE_LABELS` / `TABS` / `resolveTab` | نمایش و تب | `?tab=DROP TABLE` ← تب اول |

## ۳) امانت‌داری جابه‌جایی (چرا «رفتار عوض نشده» را می‌توان باور کرد)

* **جهت اول:** ۱۵۷۸ خط منطقِ فایل اصلی با فایل‌های جدید مقایسه شد → **۹۵٫۱٪ عیناً کاراکتر‌به‌کاراکتر** جابه‌جا شده (dedent جزئی). ۷۷ خطِ اختلاف فقط در پنج ناحیهٔ شناخته‌شده است: بلوک `import`ها، افزودن `export` به interfaceها/ثابت‌ها، امضای هوک Provider، سه `useState` پیوندپذیری، و پنج بلاکِ واگذارشده به هستهٔ خالص.
* **جهت دوم:** در ۱۷ فایل UI/مودال **هیچ خط منطق تازه‌ای اختراع نشد** — تنها خطوط تازه، نام‌های کلید context در `const { … } = useCurriculum()` هستند (۱۷۲ خط، همگی plumbing). شرط‌های رندر (`{activeTab === 'X' && detail && (…)}`, `{modal === 'X' && …}`) عمداً **داخل** فایل هر قطعه نگه داشته شد، تا نه JSX دست‌کاری شود و نه `narrowing` تایپ‌ها بشکند.
* تنها تغییر نوعی: چهار فرزندِ قاب «جزئیات نسخه» خط نگهبان `if (!selectedVersion) return null;` گرفتند — در فایل اصلی همان `narrowing` از والد (`{selectedVersion && ( … )}`) می‌آمد و والد در حالت null هیچ فرزندی رندر نمی‌کرد؛ پس رفتار یکسان است (توضیح کامل در کامنت هر فایل).

## ۴) باگ P0 که همین‌جا بیرون افتاد: `curriculum_versions.createdAt`

برای اینکه تب‌ها روی دادهٔ واقعی رندر شوند، هارنس PostgreSQL تازه‌ای با `migrate → apply-patches → seed` نوشتم. رندر تب‌های نیازمندِ «جزئیات نسخه» خالی بود و در لاگ dev این خطا بود:

```
getCurriculumVersionDetailAction: Error: Failed query: select … "createdAt" … from "curriculum_versions"
[cause]: error: column "curriculum_versions.createdAt" does not exist
```

ریشه: `schema.ts` این ستون را دارد، اما مهاجرت ۰۰۰۲ (تغییر نام `syllabuses` → `curriculum_versions`) فقط `updatedAt` را افزود و `patches.sql` هم آن را نمی‌سازد. چون `loadVersionData` با `select()` همهٔ ستون‌ها را می‌خواند، **در هر استقرارِ ساخته‌شده با زنجیرهٔ مهاجرت** (پروداکشن/CI) جزئیات نسخه همیشه `{ ok:false }` برمی‌گشت — یعنی تب‌های «دروس کاتالوگ»، «ترم‌بندی چارت»، «بررسی و خاتمه» و ذخیرهٔ پیش‌نیاز/هم‌نیاز عملاً از کار می‌افتادند. در محیط توسعه دیده نشد چون `drizzle-kit push` ساختار را از خود TypeScript می‌سازد.

رفع: مهاجرت **`drizzle/0012_curriculum_created_at.sql`** (`ADD COLUMN IF NOT EXISTS` + بازسازی مقدار از `updatedAt`) + ثبت در `meta/_journal.json`. روی دیتابیس‌های push‌شده بی‌اثر است.

برای بستن کل این خانواده، یک بازبینی سیستماتیک زدم: ساخت DB از صفر با مهاجرت‌ها + پچ‌ها، و مقایسهٔ **۱۴۱ جدول / همهٔ ستون‌های اعلام‌شده در `schema.ts`** با `information_schema`. پیش از رفع: ۶ جدول با ستونِ ساخته‌نشده (پنج‌تایشان را `patches.sql` می‌ساخت و فقط `createdAt` واقعاً گم بود). پس از رفع:

```
جدول‌های اعلام‌شده در سکما: 141 — در DB: 141
✅ سکما و دیتابیس کاملاً هم‌راستا (نصب تازه).
```

## ۵) تأییدها

| بررسی | نتیجه |
|---|---|
| `npx tsc --noEmit` | **۰ خطا** |
| `npm test` (۲۰ سوئیت) | **سبز** — ۹۳ assert هستهٔ درسی + ۱۷ رندر مستقل + سوئیت‌های قبلی |
| `node scripts/audit-actions.mjs` | ۲۴۳ اکشن، **همه گارددار** (`actions.ts` دست‌نخورده) |
| `npm run build` | **exit ۰** — فقط ۵ هشدار همیشگیِ `api/archive/[docId]` |
| مهاجرت‌ها روی DB تازه | **۱۲/۱۲** در دفتر ثبت شد و سکما هم‌راستا |
| رندر واقعی (PostgreSQL + نشست واقعی) | **۸/۸ سناریو HTTP 200، صفر خطای رندر** |
| بدون نشست | **۳۰۷ → `/login`** (نه ۵۰۰) |

فیکسچر هارنس: یک رشته + یک نسخهٔ `DRAFT` + دو درس با واحد و ترم + یک قاعدهٔ `PREREQ` در `course_rules`. نتایج رندر روی همان دادهٔ واقعی:

| سناریو | ماکرها |
|---|---|
| `/admin/curriculum` | مدیریت نسخه‌های برنامهٔ درسی · تعریف کاتالوگ رشته · جدول نسخه‌ها |
| `?tab=CATALOG&version=10` | جدول + کد نسخه `1402-1` |
| `?tab=COURSES&version=10` | «افزودن درس از بانک» · «پیش‌نیاز / هم‌نیاز» · `مبانی برنامه‌سازی` · `CM101` |
| `?tab=SEMESTERS&version=10` | «تابستان» · `ساختار داده` (درس ترم ۵ = مسیر سرریز چارت) · `CM101` |
| `?tab=VERIFY&version=10` | «🎯 سهم واحد هر نقش» · «✏️ قابل ویرایش» · `1402-1` |
| `?tab=TRANSFER&version=10` | بلوک «۱️⃣ نسخهٔ جدید (R+1) از همین نسخه» |
| `?tab=DROP TABLE` | به تب اول برمی‌گردد (اثبات زندهٔ `resolveTab`) |
| `?version=99999` | شمارهٔ ناموجود/خارج دسترس نادیده گرفته می‌شود، صفحه سالم |

نکته: مودال‌ها از URL باز نمی‌شوند؛ به‌جای آن `tests/curriculum-ui-render.test.tsx` هر پنج مودال + ویرایشگر سهم نقش + توست را با context ساختگی **واقعاً رندر** می‌کند و متن‌هایشان را بررسی می‌کند.

## ۶) افزودنی‌های رفتار (عمدی، محافظه‌کارانه)

1. `?tab=` و `?version=` — تب/نسخهٔ آغازین. مقدار نامعتبر → حالت قبلی (تب اول، بدون انتخاب نسخه). رفتار پیش‌فرض صفحه بدون پارامتر **دقیقاً** همان قبل است.
2. با `?version=` معتبر، صفحهٔ سرور `getCurriculumVersionDetailAction` را یک‌بار صدا می‌زند تا اولین رندر کامل باشد (SSR). effect سمت کلاینت بعداً همان داده را تازه می‌کند — پس فقط یک request موازی است، نه تغییر منطق.
3. `curriculum-context.ts` جدا شد تا گراف import کامپوننت‌ها به `actions.ts` (`"use server"` → `server-only`) نکشد؛ همین اجازه داد تست رندر Node‌پایین بنویسیم.

## ۷) باقی‌مانده

1. پنل دانشجویان: ۱۲۵۹ خط باقی است و تقسیم سه پنل `mainView` (با بلوک‌های در هم تنیدهٔ `info_combined`) — نیازمند قیچی دستی + تست رندر هر پنل.
2. `actions.ts` برنامهٔ درسی ۱۱۸۰ خط است (۲۴ اکشن گارددار)؛ کاندیدای بعدی تقسیم آن بر حسب «خواندنی / نوشتن چرخه حیات / ویرایش دروس و قواعد» است، با تست یکپارچگی روی DB واقعی.
3. تست یکپارچگی «ذخیرهٔ سهم واحد نقش‌ها → اعتبارسنجی مجدد» (`handleSaveRoleTargets` که دو اکشن را پشت هم می‌زند) هنوز روی DB واقعی پوشش ندارد — بهترین کاندیدا برای سوئیت بعدیِ `rls-test`-مانند.
4. هارنس‌ها بیرون از ریپو و قابل استفادهٔ مجدد‌اند: `/home/user/tmp-pg/smoke-{scheduling,students,curriculum}.mjs` و `/home/user/tmp-pg/schema-diff.mjs` (مقایسهٔ سکما با DB تازه).
