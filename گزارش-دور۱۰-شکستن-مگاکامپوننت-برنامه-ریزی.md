# گزارش دور ۱۰ — جراحی مگاکامپوننت کارتابل برنامه‌ریزی درسی (۲۳۵۸ خط → ۱۷ فایل)

**تاریخ:** ۹ سپتامبر ۲۰۲۶ — **وضعیت: ✅ کامل، با تست رندر واقعی تأیید و push شده**

گام ۴ جدول «توصیه‌های باقی‌مانده» دور ۹ — بزرگ‌ترین مگاکامپوننت پروژه.

---

## ۱) قبل و بعد

| | قبل | بعد |
|---|---|---|
| `DepartmentPlanningClient.tsx` | **۲۳۵۸ خط** (۴۰ `useState`، ۱۳ `useMemo`، ۷ تب، بنر، توست، مودال، ۲ کامپوننت کمکی، همه محاسبات) | **۳۲ خط** (فقط Provider + Shell) |
| بزرگ‌ترین فایل ماژول | ۲۳۵۸ | **۵۴۹** (فقط وضعیت و فراخوانی اکشن‌ها) |
| منطق خالص قابل تست | صفر (همه داخل `useMemo` راکت) | **۳۴۴ خط** در `planning-core.ts` + **۱۹۰ خط تست، ۸۶ assert** |
| ریسک رندر یک تب | بارگذاری/کامپایل کل ۲۳۵۸ خط | هر تب فایل مستقل خودش را دارد |

```
src/app/admin/scheduling/
├── page.tsx                    (سرور — + پارس ?tab=)
├── DepartmentPlanningClient.tsx ۳۲  ← فقط Provider + Shell
├── types.ts                   ۲۱۹  ← همهٔ interfaceها (بدون React)
├── planning-core.ts           ۳۴۴  ← توابع خالص + ثابت‌ها (TIME_SLOT_PRESETS، PHASE_LABELS، نقشه‌ها)
├── PlanningProvider.tsx       ۵۴۹  ← تنها صاحب state: ۴۰ useState + Server Actionها + مشتقات
├── actions.ts                 ۶۵۶  (دست‌نخورده)
└── components/
    ├── PlanningShell.tsx       ۳۹  ← چیدمان و انتخاب تب
    ├── PlanningHeader.tsx     ۱۶۵  ← بنر + نوار زمینه (نیمسال/رشته/ورودی/شیفت/نمای هفته)
    ├── PlanningTabsBar.tsx    ۱۰۱  ← نوار ۷ گام
    ├── PlanningToast.tsx       ۲۴
    ├── ProfessorSelect.tsx     ۴۹  ← کشویی «تنبل» (ضد OOM)
    ├── CurriculumAssignTab.tsx ۲۸۵ ← گام ۱
    ├── ProfessorQuotasTab.tsx  ۱۱۳ ← گام ۲
    ├── DeptRoomsTab.tsx         ۷۵ ← گام ۳
    ├── SmartAssignTab.tsx      ۳۰۷ ← گام ۴ (گذار فاز + تأمین گروه + پیشنهاد موتور + سلامت)
    ├── WeeklyMatrixTab.tsx     ۱۹۹ ← گام ۵ (ماتریس زنگ تفریح + بازرس استاد)
    ├── ApprovedTab.tsx          ۷۹ ← گام ۶
    ├── TermCalendarTab.tsx     ۱۵۷ ← گام ۷
    └── ProfAvailabilityModal.tsx ۸۹
```

**معماری:** state در یک `Context` (نه props) — چون تغییر نیمسال روی *هر هفت* تب اثر
می‌گذارد و دکمهٔ «برنامه ⬅️» در تب ۲، تب ۵ را هدف می‌گیرد. تب‌ها state خودی ندارند و
فقط `usePlanning()` را می‌خوانند، پس جابه‌جایی‌شان بین فایل‌ها هیچ همگام‌سازی را نمی‌شکند.

## ۲) منطق خالصی که بیرون آمد (و قبلاً غیرقابل تست بود)

| تابع | کار | تست |
|---|---|---|
| `computeProfUnits` | بار واحد هر استاد؛ درس مشترک با نسبت تئوری/عملی تقسیم می‌شود | ۷ assert (شامل استاد نامعلوم، گروه موازی، مجموع سهم) |
| `slotAvailStatus` | آیا بازهٔ اعلامی استاد این اسلات را پوشش می‌دهد؟ (روز ۱‌پایه DB ↔ شاخص ۰‌پایه UI) | ۹ assert (مرزها: `endTime == slot.start` ⇒ NONE) |
| `filterScenarioOfferings` / `filterContextDemands` | فیلتر رشته/ورودی/زوج‌وفرد (ارائهٔ عمومی `programId=0` همیشه می‌ماند) | ۸ assert |
| `inspectorSummary` | مجموع واحد، روزها، رشته‌ها، درصد سهمیه | **سقف صفر ⇒ ۰ به‌جای NaN** (باگ احتمالی قبلی بسته شد) |
| `nextGroupNumber` / `coTeachingWeights` | شمارهٔ گروه بعدی؛ کلمپ سهم ۱۰..۹۰٪ | ۷ assert |
| `parseJalaliDates` | استخراج تاریخ تعطیلات از متن آزاد | **باگ واقعی رفع شد** — فقط رقوم لاتین را می‌خواند؛ «۱۴۰۵/۰۷/۰۱» که کاربر در فیلد فارسی‌زبان تایپ می‌کند بی‌صدا نادیده گرفته می‌شد ⇒ جلسه در روز تعطیل تولید می‌شد. حالا اول `faToEnDigits` می‌شود |
| `faNum` | رقم فارسی | تایپ از `any` به `number \| string \| null \| undefined` اصلاح شد (تنها `any` صریح ماژول) |

## ۳) افزودهٔ کوچک: پیوند مستقیم به تب

`/admin/scheduling?tab=SCENARIOS` تب آغازین را انتخاب می‌کند (`resolveTab` مقدار
نامعتبر را به تب ۱ برمی‌گرداند — تست شده). هم برای کاربر مفید است (اشتراک‌گذاری لینک
یک گام)، هم هر تب را **مستقل قابل رندر و تست** می‌کند — همان چیزی که برای تأیید این
ریفکتور لازم بود. الگوی هم‌نام ماژول حقوق (`/admin/payroll?tab=simulator`).

## ۴) روش تأیید (ادعا نه، اجرا)

| گام | نتیجه |
|---|---|
| **اثبات «فقط جابه‌جایی»** — هر خط غیرخالی بازهٔ ۴۷۱..۲۳۵۸ فایل اصلی با فایل‌های جدید مقایسه شد | **۱۶۴۸ از ۱۷۲۰ خط (۹۵٫۸٪) عیناً** منتقل شد؛ ۷۲ خط باقی‌مانده فقط بندهای `activeMainTab === …`، کامنت‌های `/* === */` و همان ۶ قطعه‌ای هستند که عمداً به تابع خالص تبدیل شدند |
| `npx tsc --noEmit` | ✅ صفر خطا |
| `npm test` (۱۷ سوئیت) | ✅ سبز — شامل ۸۶ assert جدید `scheduling-planning-core.test.ts` |
| `npm run build` | ✅ ۹۲ مسیر، صفر خطا (۵ هشدار موجودِ مربوط به `fs` در `api/archive` دست‌نخورده) |
| `node scripts/audit-actions.mjs` | ✅ ۲۴۳ اکشن، همه گارددار |
| **رندر واقعی**: PostgreSQL زنده + مهاجرت ۱۲/۱۲ + seed + `next dev` + نشست واقعی | ✅ **۷/۷ تب HTTP 200** با ماکر متنی هر تب، صفر خطای رندر |
| بدون نشست | ✅ 307 → `/login` (حفاظت proxy.js سالم) |
| بررسی importهای بی‌استفاده در ۱۷ فایل جدید | ✅ صفر |

## ۵) نکتهٔ محیطی

برای اجرای تست رندر، سندباکس داکر/Redis نداشت؛ با `embedded-postgres` (PG 18.4 روی
پورت ۵۴۳۲۹) بالا آمد. **Redis لازم نشد** — این صفحه به آن وابسته نیست.

## ۶) باقی‌ماندهٔ همان دسته (هنوز شکسته نشده)

| فایل | خط | پیشنهاد |
|---|---|---|
| `admin/curriculum/CurriculumManagerClient.tsx` | ۱۸۳۱ | همان الگو (types + core + Provider + تب‌ها) |
| `admin/students/StudentsManagerClient.tsx` | ۱۷۴۵ | همان الگو |
| `admin/regulations/RegulationsClient.tsx` | ۱۲۱۱ | فقط `useReducer` + دو کامپوننت کافی است |
| `admin/templates/TemplateEngineClient.tsx` | ۱۱۳۵ | جدا کردن موتور قالب از UI |

الگو و ابزار این دور (برش بازه‌ای + محاسبهٔ خودکار متغیرهای آزاد + تست «فقط
جابه‌جایی») برای سه مورد بعدی قابل استفادهٔ مجدد است.
