# اعمال تغییرات روی سرور — دستور درست

## ⚠️ چرا `docker compose exec -T app node scripts/apply-patches.mjs` کار نمی‌کند

دو دلیل مستقل، هر کدام به‌تنهایی کافی است:

**۱) کانتینر `app` اصلاً این فایل‌ها را ندارد.**
مرحلهٔ `runner` در `afagh-next/Dockerfile` فقط سه چیز را کپی می‌کند:

```dockerfile
COPY --from=builder /app/public       ./public
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static
```

نه `scripts/` آنجاست و نه `src/db/patches.sql`. پس `node scripts/apply-patches.mjs`
داخل `app` هیچ پچی اعمال نمی‌کند.

**۲) `exec` روی ایمیج در حال اجرا کار می‌کند، یعنی کد قدیمی.**
حتی اگر فایل‌ها آنجا بودند، `exec` نسخهٔ داخل کانتینرِ فعلی را اجرا می‌کند — همان
`patches.sql` قدیمی، بدون ستون‌های جدید. «موفق» می‌نویسد چون پچ‌های قدیمی را
با موفقیت اعمال کرده است.

> پچ‌ها متعلق به سرویس **`migrator`** هستند، نه `app`. فقط مرحلهٔ `migrator`
> در Dockerfile دستور `COPY . .` دارد و کل مخزن را در خود دارد.

---

## ✅ دستور درست — یک بیلد، همه‌چیز

در ریشهٔ مخزن روی سرور:

```bash
cd ~/afagh                                   # مسیر مخزن روی سرور شما
git fetch origin arena/01a07109-afagh
git checkout -f arena/01a07109-afagh
git log --oneline -1                         # باید a2051a3 یا جدیدتر باشد

docker compose build                         # یک بیلد: app + migrator
docker compose up -d                         # migrator خودکار اجرا می‌شود
```

`docker compose up -d` سرویس `migrator` را — چون ایمیجش عوض شده — دوباره
می‌سازد و اجرا می‌کند. `migrator` این زنجیره را می‌رود:

```
backup-db → drizzle-kit push → apply-patches → seed-base → hardening
```

و `app` به‌خاطر `depends_on: migrator: service_completed_successfully` تا پایان
موفق آن بالا نمی‌آید. پس **نیازی به اجرای دستی apply-patches نیست.**

معادل کوتاه (اگر شاخه از قبل درست است):

```bash
make update      # git pull + docker compose build + up -d
```

---

## بررسی اینکه واقعاً اعمال شد

```bash
docker compose logs migrator | tail -25
```

باید «✅ پچ‌های اسکیما اعمال شد» را ببینید. سپس ستون‌های جدید را مستقیم بسنجید:

```bash
docker compose exec -T postgres psql -U afagh -d afagh_db -c '\d departments'
```

باید `headStaffId`، `kind` و `isActive` را داشته باشد. و برای ایندکس یکتای کدها:

```bash
docker compose exec -T postgres psql -U afagh -d afagh_db \
  -c "\di+ faculties_facultyCode_uq" -c "\di+ departments_departmentCode_uq"
```

---

## فقط ستون‌های دیتابیس، بدون بیلدِ اپ

اگر می‌خواهید همین حالا فقط اسکیما را جلو ببرید و بیلد سنگین `next build` را
به بعد موکول کنید — مرحلهٔ `migrator` اصلاً `next build` ندارد و بیلدش سبک است:

```bash
docker compose build migrator
docker compose run --rm migrator node scripts/apply-patches.mjs
```

⚠️ توجه: با این کار **صفحه‌های جدید بالا نمی‌آیند** (مرکز کدها، گروه‌های
آموزشی، رفع دکمه‌های بارگذاری). آن‌ها داخل باندل Next هستند و فقط با بیلد
ایمیج `app` منتشر می‌شوند. ستون‌های تازه بی‌استفاده می‌مانند ولی هیچ چیزی را
خراب نمی‌کنند (همه `ADD COLUMN IF NOT EXISTS` با مقدار پیش‌فرض‌اند).

---

## اگر `migrator` با `exit 1` تمام شد

اول همیشه لاگ را ببینید — `exit 1` بدون متن، هیچ نمی‌گوید:

```bash
docker compose logs migrator | tail -40
```

### خطای شناخته‌شده: «undefined is not iterable» در گام دادهٔ پایه

اگر لاگ چنین چیزی داشت:

```
✓ آیین‌نامه‌ها (4)
❌ خطا: undefined is not iterable (cannot read property Symbol(Symbol.iterator))
```

این باگ در کامیت `1a4a06e` رفع شده است. علتش: ایندکس یکتای تازهٔ
`departments_departmentCode_uq` باعث می‌شد `seed-base` وقتی «کد گروه» از قبل
متعلق به گروهی در دانشکدهٔ دیگر بود، به مسیر تعارض بیفتد — مسیری که خودش از
قبل شکسته بود (`[dep] = await q1(...)` روی شیء، نه آرایه).

رفع: `git pull` تازه و دوباره `docker compose build && docker compose up -d`.
حالا `seed-base` گروه واقعی شما را تصاحب نمی‌کند؛ گروه پایه را بدون کد
می‌سازد و هشدار می‌دهد تا در `/admin/codes` کد بدهید.

> نکته: پچ‌های اسکیما پیش از `seed-base` اجرا می‌شوند، پس اگر تا این‌جا رسیده
> بودید، ستون‌ها و ایندکس‌ها **اعمال شده‌اند**؛ فقط دادهٔ پایه و سخت‌سازی ناتمام
> مانده و `app` بالا نیامده است. اجرای دوباره بی‌خطر است.

---

## اگر بیلد با `exit code: 137` شکست خورد

یعنی حافظه تمام شده است (OOM)، نه خطای کد:

```bash
make swap            # ساخت swap
make build-lowmem    # بیلد با هیپ ۱۵۳۶ مگابایت
docker compose up -d
```

---

## بازگشت (rollback)

پچ‌های این نسخه همگی افزایشی‌اند و داده‌ای را حذف یا بازنویسی نمی‌کنند:

```sql
-- در صورت نیاز به بازگشت کامل اسکیما
ALTER TABLE departments DROP COLUMN IF EXISTS "headStaffId";
ALTER TABLE departments DROP COLUMN IF EXISTS "kind";
ALTER TABLE departments DROP COLUMN IF EXISTS "isActive";
DROP INDEX IF EXISTS "faculties_facultyCode_uq";
DROP INDEX IF EXISTS "departments_departmentCode_uq";
```

و برای برگشت کد:

```bash
git checkout -f <کامیت-قبلی>
docker compose build && docker compose up -d
```
