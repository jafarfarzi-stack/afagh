# گزارش دور ۷ — اجرای اپ + بررسی خطای «پروندهٔ دانشجویی یافت نشد.»

**تاریخ:** ۴ سپتامبر ۲۰۲۶ — **وضعیت: ✅ کامل و تأییدشده**

---

## ۱) نتیجهٔ اجرا

- ✅ اپ (Next.js 16، Turbopack) روی پورت **8080** بالا آمد و در حال اجراست (Live Preview).
  - محیط: `NODE_ENV=development` + `AFAGH_DEMO_MODE=1` + دیتابیس تازهٔ `afagh_db` روی PostgreSQL 18.4.
- ⚠️ نکته: `npm run build` در این سندباکس به دلیل **محدودیت حافظه (~۲GB)** با `Killed` (OOM) متوقف شد — **مشکل محیط است، نه کد**؛ در CI گیتهاب build سبز است. برای تست از `next dev` استفاده شد که کاملاً معتبر است.

## ۲) خطای «پروندهٔ دانشجویی یافت نشد.» — ریشه و فیکس

**ریشه:** در نصب تازهٔ Docker، migrator فقط اسکیما را push می‌کند (seed ندارد) → ردیف جدول `students` برای حساب دمو ساخته نمی‌شد → `getStudentByUser` مقدار null برمی‌گرداند → صفحات دانشجو پیام خطا می‌دادند.

**فیکس (از قبل در HEAD موجود است):** در `src/lib/auth.ts` تابع `ensureDemoStudentRecord`
- `ensureBaseReferenceData()` (idempotent) ← انتخاب degree/regulation (اولین) ← major پیش‌فرض کد `412`
- insert ردیف `students` با `onConflictDoNothing` هنگام ورود دمو
- کدهای کامیت: `959a5fc` (auth) و `eb227db` (صفحات graduation)

## ۳) تست واقعی روی دیتابیس تازه (بدون seed — شبیه Docker)

| تست | نتیجه |
|---|---|
| ورود `31412001 / 123456` از HTTP (Server Action واقعی) | ✅ کوکی نشست صادر شد |
| باز شدن `/student` | ✅ **صفر مورد** «پروندهٔ دانشجویی یافت نشد» — نام «علی رضایی اصل» و شمارهٔ 31412001 رندر شد |
| صفحات `finance` / `enroll` / `graduation` / `requests` | ✅ همه صفر خطا |
| وضعیت DB پس از ورود | ✅ ردیف `students` ساخته شد: studentCode=`31412001`، status=`ACTIVE`، entryYear=`1403` + دادهٔ پایه (۴ مقطع، ۴ آیین‌نامه، ۷ رشته) |
| **خودترمیمی**: حذف دستی ردیف student → ورود مجدد | ✅ ردیف دوباره ساخته شد و صفحه بدون خطا باز شد |

## ۴) تغییرات کد در این دور

- **هیچ** — فیکس از قبل کامیت شده بود (`959a5fc`، `eb227db`) و در ریموت موجود است (main/br16 = `aeec5b2`). چیزی برای push نبود.
- `next-env.d.ts` که dev خودکار تغییر داده بود، برگردانده شد؛ درخت git تمیز (۰ تغییر).

## ۵) فایل PDF دوم (`... Gemini (1).pdf` — ۳۴ صفحه)

- متن استخراج و با فایل قبلی مقایسه شد؛ **ادامهٔ همان گفتگوی طراحی** است (خلاصهٔ ۴ محور: معماری بک/مالی، امنیت و امضای دیجیتال، مقیاس‌پذیری، بهینه‌سازی فرانت).
- گام بعدی پیشنهادی سند: **رفاکتور فرانت حجیم** (شکستن مگاکامپوننت‌ها مثل `PayrollEngineClient` و `ProfessorGradesClient` + `useReducer` به‌جای `useState` + اتصال Actionهای کلاینت به Server Actions با `useActionState`/`useOptimistic`).
- ⏳ هنوز در صف است — با کاربر هماهنگ شود که ادامهٔ طراحی از این نقطه شروع شود یا کار دیگری مقدم است.

## ۶) وضعیت فعلی

- سرور dev **در حال اجرا** است → می‌توانید همین حالا در preview با `31412001 / 123456` وارد شوید.
- Postgres 18.4 هم روشن است (پورت 5432).
- ریموت: main = `aeec5b2` (همان HEAD محلی) — همهٔ کارهای قبلی push شده.
